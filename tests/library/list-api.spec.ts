import { test, expect } from '@playwright/test';

test.describe('QR List API @smoke', () => {
  test.fixme('unauthenticated GET /api/qr/list returns 401', async ({ request }) => {
    // TODO: endpoint created in plan 09-02
  });
});
