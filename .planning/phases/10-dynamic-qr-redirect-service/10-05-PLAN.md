---
phase: 10-dynamic-qr-redirect-service
plan: 05
type: execute
wave: 4
depends_on: ["10-03", "10-04"]
files_modified:
  - tests/dynamic/redirect.spec.ts
  - tests/dynamic/create-api.spec.ts
  - tests/dynamic/update-api.spec.ts
  - tests/dynamic/pause-api.spec.ts
autonomous: false
requirements: [DYN-01, DYN-02, DYN-03, DYN-04, DYN-05]

must_haves:
  truths:
    - "Redirect endpoint returns 307 for active slugs, 200 holding page for paused, 404 for invalid"
    - "Save API creates dynamic QR with slug for authenticated user"
    - "PATCH destination URL works for owner"
    - "PATCH isPaused toggles pause state"
    - "Free user at limit gets 403 dynamic_limit_reached"
    - "Full user flow: create dynamic QR in generator, see it in dashboard, edit destination, pause/activate, scan redirect"
  artifacts:
    - path: "tests/dynamic/redirect.spec.ts"
      provides: "Redirect endpoint smoke tests (activated)"
    - path: "tests/dynamic/create-api.spec.ts"
      provides: "Dynamic QR creation + tier limit tests (activated)"
    - path: "tests/dynamic/update-api.spec.ts"
      provides: "Destination update tests (activated)"
    - path: "tests/dynamic/pause-api.spec.ts"
      provides: "Pause toggle tests (activated)"
  key_links:
    - from: "tests/dynamic/redirect.spec.ts"
      to: "src/pages/r/[slug].ts"
      via: "HTTP GET requests to /r/[slug]"
      pattern: "page\\.goto.*\\/r\\/"
---

<objective>
Activate the Wave 0 test scaffolds into real smoke tests and run the full suite. Then perform human verification of the complete dynamic QR flow.

Purpose: Validates all 5 DYN requirements end-to-end. Tests provide automated regression coverage; human verification confirms visual/interactive correctness.

Output: Passing smoke test suite for Phase 10, human sign-off on dynamic QR flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-dynamic-qr-redirect-service/10-CONTEXT.md
@.planning/phases/10-dynamic-qr-redirect-service/10-RESEARCH.md
@.planning/phases/10-dynamic-qr-redirect-service/10-03-SUMMARY.md
@.planning/phases/10-dynamic-qr-redirect-service/10-04-SUMMARY.md

<interfaces>
API routes (from Plans 01 + 02):
- GET /r/[slug] — 307 redirect (active), 200 holding (paused), 404 holding (invalid)
- POST /api/qr/save — accepts isDynamic + destinationUrl, returns { id, slug }
- PATCH /api/qr/[id] — accepts destinationUrl and/or isPaused
- GET /api/qr/list — returns dynamic metadata via left join
- DELETE /api/qr/[id] — cascades to dynamicQrCodes

Test pattern (from existing tests/library/*.spec.ts):
```typescript
import { test, expect } from '@playwright/test';
// Tests use page.goto() for redirect tests, page.request for API tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate smoke tests for redirect endpoint and dynamic QR API routes</name>
  <files>tests/dynamic/redirect.spec.ts, tests/dynamic/create-api.spec.ts, tests/dynamic/update-api.spec.ts, tests/dynamic/pause-api.spec.ts</files>
  <action>
Convert test.fixme stubs to real tests. Follow the existing test patterns from `tests/library/*.spec.ts`.

**tests/dynamic/redirect.spec.ts:**
1. Test: GET `/r/nonexistent-slug` returns status 404 and response body contains "This QR code is no longer active." (holding page).
   - Use `page.goto` or `page.request.get` to hit the redirect endpoint.
   - Check `response.status() === 404` and `await response.text()` contains the heading.
2. Test: Verify holding page HTML contains expected elements (QRCraft logo text, `<h1>`, `<main>`).
3. Tests for 307 redirect and paused holding page require a seeded slug in the DB — use `test.fixme` with comment "Requires DB seeding — manually verify at checkpoint" if direct DB seeding is not feasible from Playwright. Alternatively, if the dev server has DB access, create a setup step that inserts a test row directly.

**tests/dynamic/create-api.spec.ts:**
1. Test: POST `/api/qr/save` with `isDynamic: true` without auth cookie returns 401.
   - Use `page.request.post` without authentication.
2. Keep session-dependent tests (free user limit, pro user unlimited) as `test.fixme` with comment "Requires real Clerk session — manually verify at checkpoint."

**tests/dynamic/update-api.spec.ts:**
1. Test: PATCH `/api/qr/[id]` without auth returns 401.
   - Use `page.request.fetch` with method PATCH, no auth.
2. Keep owner vs wrong-user IDOR test as `test.fixme`.

**tests/dynamic/pause-api.spec.ts:**
1. Test: PATCH `/api/qr/[id]` with isPaused without auth returns 401.
2. Keep authenticated toggle tests as `test.fixme`.

For tests that CAN run without auth (401 checks, invalid slug holding page), implement fully. For tests requiring Clerk sessions, keep as `test.fixme` per project convention (Phase 07 decision: session-bound tests cannot be automated in CI).

Run `npx playwright test tests/dynamic/ --grep @smoke` to verify all activatable tests pass and fixme tests are visible in the report.
  </action>
  <verify>
    <automated>npx playwright test tests/dynamic/ --grep @smoke 2>&1 | tail -20</automated>
  </verify>
  <done>Smoke tests for unauthenticated 401s and invalid slug holding page pass. Session-dependent tests remain test.fixme with clear reasons. No regressions in existing test suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete dynamic QR flow</name>
  <files>none</files>
  <action>
Present the user with the verification checklist below. Wait for user to test each item and report results. This is a blocking checkpoint — Phase 10 cannot be marked complete without human sign-off on all 5 DYN requirements.

What was built:
- Dynamic QR toggle in generator (URL tab only)
- Save flow creates dynamic QR with slug
- Dashboard cards show Dynamic badge, destination URL, active/paused status
- Inline destination URL editing on cards
- Pause/activate toggle on cards
- /r/[slug] redirect endpoint with holding pages
- Free user 3-QR limit with upgrade prompt

Verification steps for the user:

1. Create a dynamic QR (DYN-01): Go to generator, URL tab, enter URL, enable toggle, save, verify in dashboard
2. Verify dashboard card (DYN-02, DYN-04): Dynamic badge, destination URL, green Active status
3. Edit destination URL (DYN-02): Click pencil, change URL, save, verify toast
4. Pause/activate toggle (DYN-04): Pause, verify toast and amber status, activate, verify toast and green status
5. Test redirect (DYN-03): Visit /r/[slug], verify 307 redirect. Pause and revisit, verify holding page
6. Test invalid slug (DYN-03): Visit /r/doesnotexist, verify holding page with dark mode
7. Test free tier limit (DYN-05): As free user, create 3, try 4th, verify lock + upgrade toast
8. Verify non-URL tab (D-02): Switch to other tabs, verify toggle greyed out
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>All 5 DYN requirements verified by human testing. Phase 10 is complete.</done>
</task>

</tasks>

<verification>
- `npx playwright test tests/dynamic/ --grep @smoke` — smoke tests pass
- `npx playwright test` — full suite passes (no regressions)
- Human verification confirms all 5 DYN requirements working
</verification>

<success_criteria>
- All automated smoke tests pass
- No regressions in existing test suite
- Human confirms: dynamic QR creation, destination editing, pause toggle, redirect, holding pages, and tier limit all work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-dynamic-qr-redirect-service/10-05-SUMMARY.md`
</output>
