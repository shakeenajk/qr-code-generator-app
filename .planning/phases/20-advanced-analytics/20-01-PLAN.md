---
phase: 20-advanced-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/pages/r/[slug].ts
  - src/pages/api/analytics/[slug].ts
autonomous: true
requirements:
  - ANALYTICS-01
  - ANALYTICS-03

must_haves:
  truths:
    - "scanEvents table has utmSource, utmMedium, utmCampaign text columns"
    - "Redirect handler extracts UTM params from destination URL and stores them in scanEvents"
    - "Analytics API accepts from/to query params and filters scan data by date range"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "UTM columns on scanEvents"
      contains: "utmSource"
    - path: "src/pages/r/[slug].ts"
      provides: "UTM extraction at scan time"
      contains: "utmSource"
    - path: "src/pages/api/analytics/[slug].ts"
      provides: "Date-range-aware analytics queries plus UTM aggregation"
      contains: "utmSource"
  key_links:
    - from: "src/pages/r/[slug].ts"
      to: "src/db/schema.ts"
      via: "insert with UTM fields"
      pattern: "utmSource.*utmMedium.*utmCampaign"
    - from: "src/pages/api/analytics/[slug].ts"
      to: "src/db/schema.ts"
      via: "groupBy UTM columns"
      pattern: "scanEvents\\.utmSource"
---

<objective>
Add UTM columns to scanEvents, capture UTM params at redirect time, and extend the analytics API to support custom date ranges and UTM aggregation.

Purpose: Backend foundation for advanced analytics -- schema change, data capture, and API changes must be in place before the UI can render date-filtered data or UTM charts.
Output: Updated schema, redirect handler with UTM extraction, analytics API with date range filtering and UTM breakdown endpoint data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/db/schema.ts
@src/pages/r/[slug].ts
@src/pages/api/analytics/[slug].ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/db/schema.ts (scanEvents table, lines 77-88):
```typescript
export const scanEvents = sqliteTable('scan_events', {
  id: text('id').primaryKey().$defaultFn(() => crypto.randomUUID()),
  dynamicQrCodeId: text('dynamic_qr_code_id')
    .notNull()
    .references(() => dynamicQrCodes.id, { onDelete: 'cascade' }),
  scannedAt: integer('scanned_at').notNull().$defaultFn(() => Math.floor(Date.now() / 1000)),
  userAgent: text('user_agent'),
  country: text('country'),
  device: text('device'),
}, (table) => [
  index('scan_events_qr_id_scanned_at_idx').on(table.dynamicQrCodeId, table.scannedAt),
]);
```

From src/pages/r/[slug].ts (scan event insert, lines 49-54):
```typescript
db.insert(scanEvents).values({
  dynamicQrCodeId: row.id,
  userAgent: ua,
  country,
  device,
}).catch(() => { /* silent */ });
```

From src/pages/api/analytics/[slug].ts (existing query pattern):
- Returns JSON with: name, slug, total, unique, timeSeries, devices, countries
- Uses `gte(scanEvents.scannedAt, thirtyDaysAgo)` for time filtering
- Day bucket math: `cast(scannedAt / 86400 as int)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UTM columns to scanEvents and capture in redirect handler</name>
  <files>src/db/schema.ts, src/pages/r/[slug].ts</files>
  <read_first>src/db/schema.ts, src/pages/r/[slug].ts</read_first>
  <action>
1. In src/db/schema.ts, add three nullable text columns to the scanEvents table definition, after the `device` column:
   - `utmSource: text('utm_source')`
   - `utmMedium: text('utm_medium')`
   - `utmCampaign: text('utm_campaign')`
   Keep the existing index unchanged. No new indexes needed (UTM queries will filter by dynamicQrCodeId first, which is already indexed).

2. In src/pages/r/[slug].ts, after the `row` query succeeds and before the scan event insert:
   - Parse `row.destinationUrl` with `new URL(row.destinationUrl)`
   - Extract `utm_source`, `utm_medium`, `utm_campaign` from the URL's searchParams
   - Pass these as `utmSource`, `utmMedium`, `utmCampaign` in the `db.insert(scanEvents).values({...})` call
   - Use nullish fallback: `searchParams.get('utm_source') || null` so empty strings become null
   - Wrap URL parsing in try/catch -- if destinationUrl is malformed, set all UTM values to null (do not break the redirect)

3. Run `npx drizzle-kit push` to apply the schema change to the database.
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "utmSource" src/db/schema.ts
    - grep -q "utmMedium" src/db/schema.ts
    - grep -q "utmCampaign" src/db/schema.ts
    - grep -q "utm_source" src/pages/r/[slug].ts
    - grep -q "utmSource" src/pages/r/[slug].ts
  </acceptance_criteria>
  <done>scanEvents table has three UTM columns; redirect handler extracts UTM params from destination URL and stores them on every non-bot scan event; malformed URLs do not break redirects</done>
</task>

<task type="auto">
  <name>Task 2: Extend analytics API with date range filtering and UTM aggregation</name>
  <files>src/pages/api/analytics/[slug].ts</files>
  <read_first>src/pages/api/analytics/[slug].ts, src/db/schema.ts</read_first>
  <action>
1. Accept optional `from` and `to` query parameters (Unix timestamps in seconds) via `new URL(request.url).searchParams`. If `from` is not provided, default to 30 days ago (existing behavior). If `to` is not provided, default to now. Validate both are numbers; return 400 if non-numeric values are passed.

2. Replace the hardcoded `thirtyDaysAgo` with the parsed `from` value. Add an `lte(scanEvents.scannedAt, to)` condition alongside the existing `gte` to all time-filtered queries (timeSeries, unique scans).

3. Compute `nowBucket` from the `to` param instead of Date.now(). Compute the number of days in the range: `Math.ceil((to - from) / 86400)`. Use this to fill the timeSeries array (instead of hardcoded 30).

4. Cap timeSeries to a maximum of 365 days. If the range exceeds 365 days, return 400 with `{ error: 'date_range_too_large' }`.

5. Apply the `from`/`to` filter to the device and country queries too (not just timeSeries), so all breakdowns reflect the selected date range.

6. Add a new UTM aggregation query in the Promise.all batch -- three separate groupBy queries:
   - Group by `scanEvents.utmSource` where utmSource is not null, filtered by date range, count scans, order desc, limit 10
   - Group by `scanEvents.utmMedium` where utmMedium is not null, filtered by date range, count scans, order desc, limit 10
   - Group by `scanEvents.utmCampaign` where utmCampaign is not null, filtered by date range, count scans, order desc, limit 10

7. Add the UTM data to the response payload:
   ```typescript
   utm: {
     sources: [{ value: string, scans: number }],
     mediums: [{ value: string, scans: number }],
     campaigns: [{ value: string, scans: number }],
   }
   ```

8. Import `lte` and `isNotNull` from drizzle-orm alongside the existing imports.
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "from" src/pages/api/analytics/[slug].ts
    - grep -q "to" src/pages/api/analytics/[slug].ts
    - grep -q "utmSource" src/pages/api/analytics/[slug].ts
    - grep -q "utm" src/pages/api/analytics/[slug].ts
    - grep -q "lte" src/pages/api/analytics/[slug].ts
  </acceptance_criteria>
  <done>Analytics API accepts from/to query params for custom date ranges; device and country breakdowns are date-filtered; response includes utm.sources, utm.mediums, utm.campaigns arrays; defaults to last-30-days when no params provided (backward compatible)</done>
</task>

</tasks>

<verification>
- `npx astro check` passes with no type errors
- `grep -c "utm" src/db/schema.ts` returns 3+ (three UTM columns)
- `grep -c "utm" src/pages/r/[slug].ts` returns 3+ (UTM extraction)
- `grep -c "utm" src/pages/api/analytics/[slug].ts` returns 5+ (UTM queries + response)
- API is backward compatible: `GET /api/analytics/[slug]` without from/to returns same shape plus utm field
</verification>

<success_criteria>
- scanEvents schema has utmSource, utmMedium, utmCampaign columns
- Redirect handler extracts UTM params from destination URL query string
- Analytics API filters all queries by optional from/to date range
- Analytics API returns UTM breakdown data in response
- No type errors, no breaking changes to existing analytics
</success_criteria>

<output>
After completion, create `.planning/phases/20-advanced-analytics/20-01-SUMMARY.md`
</output>
