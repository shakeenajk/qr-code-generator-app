---
phase: 03-customization
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/customization.spec.ts
autonomous: true
requirements:
  - CUST-01
  - CUST-02
  - CUST-03
  - CUST-04
  - CUST-05
  - CUST-06
  - CUST-07
  - LOGO-01
  - LOGO-02
  - LOGO-03
  - LOGO-04

must_haves:
  truths:
    - "Smoke test file exists and all tests fail (RED) — not skipped, not passing"
    - "Every customization requirement has a corresponding @smoke test stub"
    - "All data-testid selectors used in tests match the contract defined in 03-VALIDATION.md"
  artifacts:
    - path: "tests/customization.spec.ts"
      provides: "Failing smoke stubs for CUST-01 through CUST-07 and LOGO-01 through LOGO-04"
      contains: "@smoke"
  key_links:
    - from: "tests/customization.spec.ts"
      to: "src/components/customize/"
      via: "data-testid selectors"
      pattern: "data-testid="
---

<objective>
Create a failing smoke test file (TDD RED phase) covering all 11 customization requirements. Tests must fail because the implementation does not exist yet. Establishes the data-testid selector contract that all Wave 1 sub-components must expose.

Purpose: Nyquist compliance — every Wave 1 and Wave 2 task has an automated verify command. Tests go GREEN when plans 02-05 complete.
Output: `tests/customization.spec.ts` with 10 failing @smoke tests (LOGO-03 is manual-only).
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-customization/03-CONTEXT.md
@.planning/phases/03-customization/03-RESEARCH.md
@.planning/phases/03-customization/03-VALIDATION.md
@.planning/phases/02-core-generator/02-03-SUMMARY.md
@tests/generator.spec.ts
@playwright.config.ts
</context>

<interfaces>
<!-- Established patterns from existing test suite — executor must follow these exactly. -->

From tests/generator.spec.ts:
- Test names embed requirement ID using format: `@smoke PREV-01: ...`
- Selector pattern: `page.locator('[data-testid="..."]')`
- Selector pattern for panels: `page.locator('[data-tab-panel="url"]')`
- All tests use `test.beforeEach(() => page.goto('/'))` via shared fixture

From playwright.config.ts:
- webServer: `npm run preview` on port 4321
- `npm run test:smoke` runs `--grep @smoke`
- Three browsers: chromium, firefox, webkit

<!-- data-testid contract for Phase 3 components (from 03-VALIDATION.md) -->
Selector contract (MUST match implementation exactly):
- `data-testid="color-fg"` — foreground color picker container
- `data-testid="color-bg"` — background color picker container
- `data-testid="gradient-toggle"` — gradient enable/disable toggle
- `data-testid="gradient-type"` — linear/radial selector (only visible when gradient enabled)
- `data-testid="low-contrast-warning"` — warning banner (conditional)
- `data-testid="dot-shape-square"` — shape thumbnail button
- `data-testid="dot-shape-dots"` — shape thumbnail button
- `data-testid="dot-shape-rounded"` — shape thumbnail button (default selected)
- `data-testid="dot-shape-extra-rounded"` — shape thumbnail button
- `data-testid="dot-shape-classy"` — shape thumbnail button
- `data-testid="dot-shape-classy-rounded"` — shape thumbnail button
- `data-testid="corner-frame-square"` — frame thumbnail button
- `data-testid="corner-frame-extra-rounded"` — frame thumbnail button (default selected)
- `data-testid="corner-frame-dot"` — frame thumbnail button
- `data-testid="corner-pupil-square"` — pupil thumbnail button (default selected)
- `data-testid="corner-pupil-dot"` — pupil thumbnail button
- `data-testid="corner-pupil-extra-rounded"` — pupil thumbnail button
- `data-testid="logo-dropzone"` — drag-and-drop upload zone
- `data-testid="logo-thumbnail"` — thumbnail shown after upload
- `data-testid="logo-remove"` — remove logo button
- `data-testid="logo-ecl-notice"` — "Error correction set to H" info note
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write failing customization smoke tests (Wave 0 / TDD RED)</name>
  <files>tests/customization.spec.ts</files>
  <action>
Create `tests/customization.spec.ts` following the exact conventions from `tests/generator.spec.ts`:
- Import `{ test, expect }` from `@playwright/test`
- `test.beforeEach(({ page }) => page.goto('/'))`
- Embed requirement ID in test name using `@smoke CUST-01:` format

Write one test per requirement ID (10 tests total — LOGO-03 is manual-only):

**CUST-01: Foreground color picker exists**
```
test('@smoke CUST-01: foreground color picker is visible', ...)
  page.locator('[data-testid="color-fg"]').isVisible()
```

**CUST-02: Background color picker exists**
```
test('@smoke CUST-02: background color picker is visible', ...)
  page.locator('[data-testid="color-bg"]').isVisible()
```

**CUST-03: Gradient toggle and type selector**
```
test('@smoke CUST-03: gradient toggle is visible; type selector appears when enabled', ...)
  Assert gradient-toggle is visible
  Assert gradient-type is NOT visible initially (off by default)
  Click gradient-toggle
  Assert gradient-type IS visible after toggle
```

**CUST-04: Dot shape thumbnails**
```
test('@smoke CUST-04: all six dot shape thumbnails are present', ...)
  Assert each of: dot-shape-square, dot-shape-dots, dot-shape-rounded,
    dot-shape-extra-rounded, dot-shape-classy, dot-shape-classy-rounded is visible
```

**CUST-05: Corner frame thumbnails**
```
test('@smoke CUST-05: all three corner frame thumbnails are present', ...)
  Assert each of: corner-frame-square, corner-frame-extra-rounded, corner-frame-dot is visible
```

**CUST-06: Corner pupil thumbnails**
```
test('@smoke CUST-06: all three corner pupil thumbnails are present', ...)
  Assert each of: corner-pupil-square, corner-pupil-dot, corner-pupil-extra-rounded is visible
```

**CUST-07: Low-contrast warning**
```
test('@smoke CUST-07: low-contrast warning hidden initially; appears with bad colors', ...)
  Assert low-contrast-warning is NOT visible initially (defaults are fine: #1e293b on #ffffff)
  Fill color-fg hex input with '#ffff00' (yellow — low contrast on white)
  Assert low-contrast-warning IS visible
```

For CUST-07, note: the hex input inside color-fg container can be targeted with
`page.locator('[data-testid="color-fg"] input[type="text"]')`

**LOGO-01: Logo drop zone exists**
```
test('@smoke LOGO-01: logo drop zone is visible', ...)
  Assert logo-dropzone is visible
```

**LOGO-02 + LOGO-04: ECL notice appears/disappears with logo**
```
test('@smoke LOGO-02: ECL notice shown when logo uploaded; LOGO-04: hidden when removed', ...)
  Assert logo-ecl-notice NOT visible initially
  Use page.setInputFiles('[data-testid="logo-dropzone"] input[type="file"]', {
    name: 'test.png',
    mimeType: 'image/png',
    buffer: Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==', 'base64'),
  })
  Assert logo-thumbnail is visible
  Assert logo-ecl-notice IS visible
  Click logo-remove
  Assert logo-ecl-notice NOT visible
  Assert logo-thumbnail NOT visible
```

These tests will fail when run because the components do not exist yet. Confirm they fail by running `npm run test:smoke -- customization` after creation.
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx playwright test --grep "@smoke" tests/customization.spec.ts --reporter=line 2>&1 | tail -5</automated>
  </verify>
  <done>
`tests/customization.spec.ts` exists with 10 @smoke tests. Tests run (they fail with "locator not found" or similar — NOT "syntax error"). Output shows "10 failed" with no TypeScript compilation errors.
  </done>
</task>

</tasks>

<verification>
Run `npm run test:smoke` — should show all existing 57 tests still green, plus the 10 new customization tests failing (TDD RED). Total: 57 passing + 10 failing.

Confirm the test file compiles clean: `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- `tests/customization.spec.ts` exists
- 10 @smoke tests present, one per requirement (CUST-01 through CUST-07, LOGO-01, LOGO-02+04 combined, LOGO-01)
- All 10 fail when run (RED state) — not skipped, no TS errors
- Selector names exactly match the data-testid contract in 03-VALIDATION.md
- Existing 57 smoke tests unaffected (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-customization/03-01-SUMMARY.md`
</output>
