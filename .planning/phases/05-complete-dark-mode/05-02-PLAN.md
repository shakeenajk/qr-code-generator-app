---
phase: 05-complete-dark-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/Features.astro
  - src/components/FAQ.astro
autonomous: true
requirements:
  - BRAND-04
gap_closure: true

must_haves:
  truths:
    - "Features section renders with a dark slate background (not white/gray-50) when OS dark mode is active"
    - "Features section cards render with dark:bg-slate-800 and dark:border-slate-700 in dark mode"
    - "Features section headings and body text are legible (dark:text-white / dark:text-slate-400) in dark mode"
    - "FAQ section renders with dark:bg-[#0f172a] (deepest bg, blends with body) in dark mode"
    - "FAQ item cards render with dark:bg-slate-800 and dark:border-slate-700 in dark mode"
    - "FAQ headings and answer text are legible in dark mode"
    - "Both new Playwright smoke tests from Plan 01 turn GREEN"
  artifacts:
    - path: "src/components/Features.astro"
      provides: "Features section with full dark: class coverage"
      contains: "dark:bg-slate-900"
    - path: "src/components/FAQ.astro"
      provides: "FAQ section with full dark: class coverage"
      contains: "dark:bg-[#0f172a]"
  key_links:
    - from: "src/components/Features.astro section"
      to: "dark:bg-slate-900"
      via: "Tailwind v4 prefers-color-scheme media query"
      pattern: "dark:bg-slate-900"
    - from: "src/components/FAQ.astro section"
      to: "dark:bg-[#0f172a]"
      via: "Tailwind v4 prefers-color-scheme media query"
      pattern: "dark:bg-\\[#0f172a\\]"
---

<objective>
Add dark: Tailwind classes to Features.astro and FAQ.astro, turning the two failing smoke stubs from Plan 01 GREEN.

Purpose: Close the BRAND-04 gap — Features and FAQ sections currently render as white/gray boxes in dark mode, breaking visual continuity between the dark Hero and dark Footer.
Output: Updated Features.astro and FAQ.astro with complete dark: coverage matching the Phase 4 dark palette.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-complete-dark-mode/05-RESEARCH.md
@.planning/phases/05-complete-dark-mode/05-01-SUMMARY.md

<interfaces>
<!-- EXACT CURRENT MARKUP — add dark: variants to these classes only. Do NOT restructure. -->

<!-- src/components/Features.astro (lines 38–53) -->
```astro
<section class="bg-gray-50 py-16 sm:py-20" id="features">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-gray-900 text-center mb-12">
      Everything You Need
    </h2>
    <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6" role="list">
      {features.map((f) => (
        <li class="bg-white rounded-xl border border-gray-200 p-6">
          <span class="text-3xl mb-3 block" aria-hidden="true">{f.icon}</span>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">{f.title}</h3>
          <p class="text-gray-600 text-sm leading-relaxed">{f.description}</p>
        </li>
      ))}
    </ul>
  </div>
</section>
```

<!-- src/components/FAQ.astro (lines 6–27) -->
```astro
<section class="bg-white py-16 sm:py-20" id="faq" aria-labelledby="faq-heading">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 id="faq-heading" class="text-3xl font-bold text-gray-900 text-center mb-12">
      Frequently Asked Questions
    </h2>
    <dl class="space-y-6">
      {FAQ_ITEMS.map((item) => (
        <div class="border border-gray-200 rounded-xl p-6 bg-gray-50">
          <dt class="text-base font-semibold text-gray-900 mb-2" data-faq-question>
            {item.question}
          </dt>
          <dd class="text-gray-600 text-sm leading-relaxed">
            {item.answer}
          </dd>
        </div>
      ))}
    </dl>
  </div>
</section>
```

<!-- DARK PALETTE (established in Phase 4 — must match exactly) -->
<!-- Section bg (gray-50):      dark:bg-slate-900         -->
<!-- Section bg (white):        dark:bg-[#0f172a]          -->
<!-- Card bg (white or gray-50 inset): dark:bg-slate-800  -->
<!-- Border (gray-200):         dark:border-slate-700      -->
<!-- Heading (gray-900):        dark:text-white            -->
<!-- Body text (gray-600):      dark:text-slate-400        -->

<!-- CRITICAL: FAQ section bg is bg-white — map to dark:bg-[#0f172a] (deepest bg, same as body) -->
<!--           NOT dark:bg-slate-900 — the Hero uses slate-900 for a slightly elevated surface -->
<!--           FAQ must blend with the body background for visual continuity. -->
<!-- CRITICAL: Do NOT use dark:bg-gray-800 or dark:bg-gray-900 — entire project uses slate scale. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark: classes to Features.astro</name>
  <files>src/components/Features.astro</files>
  <action>
    Read src/components/Features.astro. Apply the following dark: class additions — one per element, inline with the existing light-mode classes. Do NOT change any existing classes, markup structure, content, or the frontmatter JS block.

    Class mapping to apply:
    - section: add `dark:bg-slate-900` after `bg-gray-50`
    - h2: add `dark:text-white` after `text-gray-900`
    - li (each card): add `dark:bg-slate-800` after `bg-white`, add `dark:border-slate-700` after `border-gray-200`
    - h3: add `dark:text-white` after `text-gray-900`
    - p: add `dark:text-slate-400` after `text-gray-600`

    Result for the section tag: `class="bg-gray-50 dark:bg-slate-900 py-16 sm:py-20"`
    Result for the h2: `class="text-3xl font-bold text-gray-900 dark:text-white text-center mb-12"`
    Result for the li: `class="bg-white dark:bg-slate-800 rounded-xl border border-gray-200 dark:border-slate-700 p-6"`
    Result for h3: `class="text-lg font-semibold text-gray-900 dark:text-white mb-2"`
    Result for p: `class="text-gray-600 dark:text-slate-400 text-sm leading-relaxed"`

    After editing, build and run the features dark mode smoke test to confirm GREEN:
    ```
    npm run build && npx playwright test tests/export.spec.ts --grep "features.*dark" --project=chromium
    ```
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/export.spec.ts --grep "features.*dark" --project=chromium</automated>
  </verify>
  <done>
    - src/components/Features.astro contains dark:bg-slate-900, dark:bg-slate-800, dark:border-slate-700, dark:text-white (×2), dark:text-slate-400
    - The "features section has dark background in dark mode" Playwright test PASSES (GREEN)
    - No existing classes were removed or reordered
    - npm run build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dark: classes to FAQ.astro</name>
  <files>src/components/FAQ.astro</files>
  <action>
    Read src/components/FAQ.astro. Apply the following dark: class additions. Do NOT change any existing classes, markup structure, content, the import statement, or the data-faq-question attribute (it is a Playwright selector — must be preserved exactly).

    Class mapping to apply:
    - section: add `dark:bg-[#0f172a]` after `bg-white` (NOT dark:bg-slate-900 — FAQ section must blend with body background, same as #0f172a body value)
    - h2: add `dark:text-white` after `text-gray-900`
    - div (each FAQ item wrapper): add `dark:border-slate-700` after `border-gray-200`, add `dark:bg-slate-800` after `bg-gray-50`
    - dt: add `dark:text-white` after `text-gray-900` in the class attribute
    - dd: add `dark:text-slate-400` after `text-gray-600`

    Result for the section tag: `class="bg-white dark:bg-[#0f172a] py-16 sm:py-20"`
    Result for h2: `class="text-3xl font-bold text-gray-900 dark:text-white text-center mb-12"`
    Result for div item: `class="border border-gray-200 dark:border-slate-700 rounded-xl p-6 bg-gray-50 dark:bg-slate-800"`
    Result for dt: `class="text-base font-semibold text-gray-900 dark:text-white mb-2"`
    Result for dd: `class="text-gray-600 dark:text-slate-400 text-sm leading-relaxed"`

    After editing, build and run the FAQ dark mode smoke test:
    ```
    npm run build && npx playwright test tests/export.spec.ts --grep "faq.*dark" --project=chromium
    ```
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/export.spec.ts --grep "faq.*dark" --project=chromium</automated>
  </verify>
  <done>
    - src/components/FAQ.astro contains dark:bg-[#0f172a], dark:border-slate-700, dark:bg-slate-800, dark:text-white (×2), dark:text-slate-400
    - The "faq section has dark background in dark mode" Playwright test PASSES (GREEN)
    - data-faq-question attribute is unchanged on all dt elements
    - npm run build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Full smoke suite regression check</name>
  <files></files>
  <action>
    Run the full smoke suite across all specs to confirm both new tests are GREEN and no regressions were introduced by the dark: class additions:
    ```
    npx playwright test --grep "@smoke" --project=chromium
    ```
    If any previously-passing test fails, investigate before marking this task done.
  </action>
  <verify>
    <automated>npx playwright test --grep "@smoke" --project=chromium</automated>
  </verify>
  <done>
    All @smoke tests pass, including the two new features/faq dark mode tests. Zero regressions.
  </done>
</task>

</tasks>

<verification>
After this plan:
1. `npx playwright test tests/export.spec.ts --grep "features.*dark" --project=chromium` — PASSES
2. `npx playwright test tests/export.spec.ts --grep "faq.*dark" --project=chromium` — PASSES
3. `npx playwright test --grep "@smoke" --project=chromium` — ALL PASS (no regressions)
</verification>

<success_criteria>
1. Features.astro has dark: classes on section, h2, li cards (bg + border), h3, and p
2. FAQ.astro has dark: classes on section, h2, div items (border + bg), dt, and dd
3. Both new Playwright dark mode smoke tests are GREEN
4. Full @smoke suite has zero regressions
5. npm run build succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-complete-dark-mode/05-02-SUMMARY.md`
</output>
