---
phase: 02-core-generator
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - tests/generator.spec.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, PREV-01, PREV-02, PREV-03]

must_haves:
  truths:
    - "qr-code-styling is listed in package.json dependencies"
    - "npm run build exits 0 after install"
    - "tests/generator.spec.ts exists with 8 @smoke test stubs"
    - "All 8 stubs reference correct data-* selectors that Wave 3 will implement"
    - "Stubs are expected to fail at this stage (no implementation yet)"
  artifacts:
    - path: "package.json"
      provides: "qr-code-styling dependency"
      contains: "qr-code-styling"
    - path: "tests/generator.spec.ts"
      provides: "8 @smoke test stubs covering all Phase 2 requirements"
      contains: "@smoke"
  key_links:
    - from: "tests/generator.spec.ts"
      to: "src/components/QRGeneratorIsland.tsx"
      via: "data-* selectors defined in stubs, implemented in Wave 3"
      pattern: "data-testid"
---

<objective>
Install `qr-code-styling` and create the Wave 0 test stubs in `tests/generator.spec.ts`. The stubs will fail until Wave 3 implements the island — that is expected. Their purpose is to define the contract (selectors, behaviors) that implementation must satisfy.

Purpose: Establish the test contract and install the only new dependency for Phase 2. No other plan can proceed without the dependency installed and test stubs in place.
Output: `qr-code-styling` in `node_modules`, 8 @smoke stubs in `tests/generator.spec.ts`.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-generator/02-RESEARCH.md
@.planning/phases/02-core-generator/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 2-01-01: Install qr-code-styling</name>
  <files>package.json</files>
  <action>
Run from the repo root (`/Users/ranjit/Documents/Development/qr-generator`):

```bash
npm install qr-code-styling
```

This installs `qr-code-styling@1.8.3` (or latest compatible). After install, verify it appears in `package.json` under `dependencies`.

Do NOT install `use-debounce` or any other debounce library — debounce is implemented inline with `useEffect` + `setTimeout` as decided in research.
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
`npm run build` exits 0. `package.json` contains `"qr-code-styling"` in dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2-01-02: Create generator test stubs</name>
  <files>tests/generator.spec.ts</files>
  <action>
Create `tests/generator.spec.ts` with 8 @smoke test stubs. These stubs define the selector contract Wave 3 must implement. All stubs are expected to FAIL at this stage.

```typescript
import { test, expect } from '@playwright/test';

// Wave 0 stubs — all expected to fail until Phase 2 Wave 3 is implemented
// These define the selector contract for QRGeneratorIsland

test.describe('QR Generator @smoke', () => {

  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });

  // CONT-01: URL input produces a QR code
  test('CONT-01: URL tab generates a QR code from a URL @smoke', async ({ page }) => {
    await page.click('[data-tab="url"]');
    await page.fill('[data-tab-panel="url"] input[type="url"], [data-tab-panel="url"] input[type="text"]', 'https://example.com');
    await page.waitForTimeout(500); // wait for debounce
    const qrPreview = page.locator('[data-testid="qr-preview"]');
    await expect(qrPreview).toBeVisible();
    // QR rendered: SVG or canvas child present, placeholder hidden
    await expect(qrPreview.locator('svg, canvas')).toBeVisible();
  });

  // CONT-02: Plain text input produces a QR code
  test('CONT-02: Text tab generates a QR code from plain text @smoke', async ({ page }) => {
    await page.click('[data-tab="text"]');
    await page.fill('[data-tab-panel="text"] textarea', 'Hello, world!');
    await page.waitForTimeout(500);
    const qrPreview = page.locator('[data-testid="qr-preview"]');
    await expect(qrPreview.locator('svg, canvas')).toBeVisible();
  });

  // CONT-03: WiFi form produces a QR code
  test('CONT-03: WiFi tab generates a QR code from credentials @smoke', async ({ page }) => {
    await page.click('[data-tab="wifi"]');
    await page.fill('[data-tab-panel="wifi"] [name="ssid"]', 'MyNetwork');
    await page.fill('[data-tab-panel="wifi"] [name="password"]', 'MyPass123');
    await page.waitForTimeout(500);
    const qrPreview = page.locator('[data-testid="qr-preview"]');
    await expect(qrPreview.locator('svg, canvas')).toBeVisible();
  });

  // CONT-04: vCard form produces a QR code
  test('CONT-04: vCard tab generates a QR code from contact info @smoke', async ({ page }) => {
    await page.click('[data-tab="vcard"]');
    await page.fill('[data-tab-panel="vcard"] [name="name"]', 'Jane Doe');
    await page.waitForTimeout(500);
    const qrPreview = page.locator('[data-testid="qr-preview"]');
    await expect(qrPreview.locator('svg, canvas')).toBeVisible();
  });

  // CONT-05: Switching tabs preserves previously entered values
  test('CONT-05: Tab state is preserved when switching between tabs @smoke', async ({ page }) => {
    // Enter URL
    await page.click('[data-tab="url"]');
    await page.fill('[data-tab-panel="url"] input[type="url"], [data-tab-panel="url"] input[type="text"]', 'https://example.com');
    // Switch to text tab
    await page.click('[data-tab="text"]');
    await page.fill('[data-tab-panel="text"] textarea', 'some text');
    // Switch back to URL tab — value must be preserved
    await page.click('[data-tab="url"]');
    const urlInput = page.locator('[data-tab-panel="url"] input[type="url"], [data-tab-panel="url"] input[type="text"]');
    await expect(urlInput).toHaveValue('https://example.com');
  });

  // PREV-01: Typing triggers QR update without button press (within 1s)
  test('PREV-01: QR preview updates automatically after typing (no button required) @smoke', async ({ page }) => {
    await page.click('[data-tab="url"]');
    const qrPreview = page.locator('[data-testid="qr-preview"]');
    // Start with empty — placeholder shown
    await expect(page.locator('[data-testid="qr-placeholder"]')).toBeVisible();
    // Type a URL — QR should appear within 1s (300ms debounce + render time)
    await page.fill('[data-tab-panel="url"] input[type="url"], [data-tab-panel="url"] input[type="text"]', 'https://example.com');
    await expect(qrPreview.locator('svg, canvas')).toBeVisible({ timeout: 1000 });
  });

  // PREV-02: Preview container size does not change as QR version updates
  test('PREV-02: QR preview container stays fixed at 256x256px @smoke', async ({ page }) => {
    const qrPreview = page.locator('[data-testid="qr-preview"]');
    const initialBox = await qrPreview.boundingBox();
    expect(initialBox?.width).toBe(256);
    expect(initialBox?.height).toBe(256);
    // Type to generate QR — size must not change
    await page.click('[data-tab="url"]');
    await page.fill('[data-tab-panel="url"] input[type="url"], [data-tab-panel="url"] input[type="text"]', 'https://example.com');
    await page.waitForTimeout(500);
    const afterBox = await qrPreview.boundingBox();
    expect(afterBox?.width).toBe(256);
    expect(afterBox?.height).toBe(256);
  });

  // PREV-03: Empty input shows placeholder state
  test('PREV-03: Empty input shows ghost placeholder, not a broken QR @smoke', async ({ page }) => {
    // On initial load, no content — placeholder must be visible
    await expect(page.locator('[data-testid="qr-placeholder"]')).toBeVisible();
    // Enter content to generate QR
    await page.click('[data-tab="url"]');
    await page.fill('[data-tab-panel="url"] input[type="url"], [data-tab-panel="url"] input[type="text"]', 'https://example.com');
    await page.waitForTimeout(500);
    // Clear content — placeholder must reappear
    await page.fill('[data-tab-panel="url"] input[type="url"], [data-tab-panel="url"] input[type="text"]', '');
    await page.waitForTimeout(500);
    await expect(page.locator('[data-testid="qr-placeholder"]')).toBeVisible();
  });

});
```

**Required selectors** (Wave 3 must implement these exactly):
- `[data-tab="url"]`, `[data-tab="text"]`, `[data-tab="wifi"]`, `[data-tab="vcard"]` — tab buttons
- `[data-tab-panel="url"]`, `[data-tab-panel="text"]`, `[data-tab-panel="wifi"]`, `[data-tab-panel="vcard"]` — panel wrappers
- `[data-testid="qr-preview"]` — outer QR container (always 256×256)
- `[data-testid="qr-placeholder"]` — ghost placeholder element
- `[name="ssid"]`, `[name="password"]` — WiFi fields (inside `[data-tab-panel="wifi"]`)
- `[name="name"]` — vCard name field (inside `[data-tab-panel="vcard"]`)
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && ls tests/generator.spec.ts && grep -c "@smoke" tests/generator.spec.ts</automated>
  </verify>
  <done>
`tests/generator.spec.ts` exists. `grep -c "@smoke"` returns 8 (one per test). File contains all required `data-*` selector references.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `package.json` contains `"qr-code-styling"` in dependencies
2. `npm run build` exits 0
3. `tests/generator.spec.ts` exists with exactly 8 @smoke tags
4. All required data-* selectors are referenced in the stubs
</verification>

<success_criteria>
- `qr-code-styling` installed and build passes
- 8 @smoke test stubs exist in `tests/generator.spec.ts`
- Selectors match the contract Wave 3 will implement: `[data-tab="..."]`, `[data-tab-panel="..."]`, `[data-testid="qr-preview"]`, `[data-testid="qr-placeholder"]`
- Stubs are self-documenting: each references the requirement ID it covers
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-generator/02-01-SUMMARY.md`
</output>
