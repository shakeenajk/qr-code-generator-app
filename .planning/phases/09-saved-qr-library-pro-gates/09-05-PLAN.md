---
phase: 09-saved-qr-library-pro-gates
plan: 05
type: execute
wave: 4
depends_on:
  - 09-03
  - 09-04
files_modified:
  - tests/library/save-api.spec.ts
  - tests/library/list-api.spec.ts
  - tests/library/update-api.spec.ts
  - tests/library/delete-api.spec.ts
  - tests/gates/pro-gates.spec.ts
autonomous: false
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-04
  - GATE-01
  - GATE-02
  - GATE-03

must_haves:
  truths:
    - "All 5 test files have real assertions replacing the test.fixme stubs"
    - "`npm run test:smoke` exits 0 with all 8 smoke tests passing (not fixme)"
    - "`npm run test` (full suite) exits 0 with no regressions to existing tests"
    - "Pro user can save a QR code and see it in /dashboard (human-verified)"
    - "Anonymous user sees no locks and no Save button on the homepage (human-verified)"
    - "Authenticated non-Pro sees disabled Save button and lock overlays (human-verified)"
  artifacts:
    - path: "tests/library/save-api.spec.ts"
      provides: "Real assertions: 401 unauth, 403 non-Pro"
    - path: "tests/library/list-api.spec.ts"
      provides: "Real assertion: 401 unauth"
    - path: "tests/library/update-api.spec.ts"
      provides: "Real assertion: 401 unauth"
    - path: "tests/library/delete-api.spec.ts"
      provides: "Real assertion: 401 unauth"
    - path: "tests/gates/pro-gates.spec.ts"
      provides: "Real assertions: anonymous user path — no save button, no lock overlays"
  key_links:
    - from: "tests/library/save-api.spec.ts"
      to: "src/pages/api/qr/save.ts"
      via: "POST request without auth headers"
      pattern: "expect.*401"
    - from: "tests/gates/pro-gates.spec.ts"
      to: "src/components/QRGeneratorIsland.tsx"
      via: "Playwright page visit + DOM assertions"
      pattern: "expect.*not.*visible"
---

<objective>
Activate all Wave 0 test stubs into real passing smoke tests, run the full suite to confirm no regressions, then pause for human verification of the Pro/anonymous UX flows.

Purpose: Phase 9 gate — all automated tests green, then human spot-checks the end-to-end saved library flow and gate behavior.
Output: 5 updated test files, full suite green, human sign-off.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-saved-qr-library-pro-gates/09-CONTEXT.md
@.planning/phases/09-saved-qr-library-pro-gates/09-VALIDATION.md
@.planning/phases/09-saved-qr-library-pro-gates/09-03-SUMMARY.md
@.planning/phases/09-saved-qr-library-pro-gates/09-04-SUMMARY.md

<interfaces>
<!-- Playwright config — established patterns -->
<!-- webServer: astro dev (not preview) — Phase 7 decision -->
<!-- Smoke tag: @smoke in test name string -->
<!-- API test pattern: use page.request.fetch or request fixture -->

<!-- Example API smoke test pattern from existing Phase 8 tests: -->
```typescript
import { test, expect } from '@playwright/test';

test('Save API — unauthenticated returns 401 @smoke', async ({ request }) => {
  const response = await request.post('/api/qr/save', {
    data: { name: 'test', contentType: 'url', contentData: 'https://example.com', styleData: '{}' },
  });
  expect(response.status()).toBe(401);
});
```

<!-- Anonymous UI gate test pattern -->
```typescript
test('Pro gates — anonymous user sees no save button @smoke', async ({ page }) => {
  await page.goto('/');
  // Wait for island to hydrate
  await page.waitForLoadState('networkidle');
  // Save button should NOT be present for anonymous
  await expect(page.getByRole('button', { name: /save to library/i })).not.toBeVisible();
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate all test.fixme stubs into real assertions</name>
  <files>
    tests/library/save-api.spec.ts,
    tests/library/list-api.spec.ts,
    tests/library/update-api.spec.ts,
    tests/library/delete-api.spec.ts,
    tests/gates/pro-gates.spec.ts
  </files>
  <action>
Replace ALL `test.fixme` stubs with real `test` assertions. Do NOT use test.fixme in any of these files after this task.

**tests/library/save-api.spec.ts:**
- Test 1 (unauthenticated 401): POST /api/qr/save with valid body but no auth cookie → expect 401. Use Playwright `request` fixture. No auth headers.
- Test 2 (non-Pro 403): This test requires a signed-in non-Pro session which cannot be automated without a real Clerk session. Convert this to a `test.fixme` with comment "// Requires real non-Pro Clerk session — manually verified in 09-05 checkpoint". The unauthenticated 401 test becomes a real test.

**tests/library/list-api.spec.ts:**
- Test (unauthenticated 401): GET /api/qr/list with no auth → expect 401.

**tests/library/update-api.spec.ts:**
- Test (unauthenticated 401): PUT /api/qr/fake-id with no auth → expect 401.

**tests/library/delete-api.spec.ts:**
- Test (unauthenticated 401): DELETE /api/qr/fake-id with no auth → expect 401.

**tests/gates/pro-gates.spec.ts:**
- Test 1 (GATE-01, GATE-03 — anonymous no save button): `page.goto('/')`, wait for networkidle, assert Save to Library button is NOT visible (use `getByRole('button', { name: /save to library/i })` or `getByTestId` if a data-testid was added).
- Test 2 (GATE-02 — anonymous classy shape unlocked): `page.goto('/')`, wait for networkidle, navigate to shape section, assert classy dot shape button is visible and has no lock overlay (check for absence of aria-label "Pro feature" or absence of a Lock icon sibling). Use the dot shape button locator from the existing shapeSection UI.
- Test 3 (GATE-01 — anonymous logo drop-zone): `page.goto('/')`, wait for networkidle, assert the logo upload drop-zone is visible (not the locked state). Look for the file input or drop-zone container.

For all UI gate tests: add `await page.waitForTimeout(1000)` after networkidle to allow Clerk hydration to complete (conservative wait; Clerk resolves in ~200-400ms). This prevents false positives from checking before Clerk loads.

Run `npm run test:smoke` after writing each file to confirm it passes before moving on.
  </action>
  <verify>
    <automated>npm run test:smoke 2>&1 | tail -20</automated>
  </verify>
  <done>
`npm run test:smoke` exits 0. All @smoke tests show as "passed" (not fixme). The non-Pro 403 test remains as fixme with a clear comment. No regressions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 9 implementation:
- QR CRUD API (POST save, GET list, PUT update, DELETE delete) with Pro gate + IDOR protection
- SaveQRModal component with name input
- QRGeneratorIsland auth-awareness: tier fetch, save button (3 states), edit-mode banner
- ShapeSection Pro lock overlays on classy/classy-rounded
- LogoSection Pro lock state for authenticated non-Pro users
- QRLibrary dashboard component: grid/list toggle, empty state, cards with Edit + Delete
- Smoke suite: all unauthenticated API checks passing, anonymous gate checks passing
  </what-built>
  <how-to-verify>
**Test 1 — Anonymous user (open homepage in private/incognito window):**
1. Visit http://localhost:4321/
2. Confirm: No "Save to Library" button visible anywhere near the export buttons
3. Confirm: All dot shapes are clickable with no lock icon overlays
4. Confirm: Logo upload section shows the standard drop-zone (not a locked state)
5. Confirm: QR generation works normally

**Test 2 — Authenticated non-Pro user (sign in with a free/starter account):**
1. Sign in at http://localhost:4321/login
2. Navigate to http://localhost:4321/
3. Confirm: "Save to Library" button is visible but greyed out with a lock icon
4. Click the greyed-out save button → confirm an upgrade prompt or message appears
5. Scroll to dot shapes → confirm "classy" and "classy-rounded" tiles have a lock/padlock overlay
6. Click a locked shape → confirm an upgrade prompt appears (not the shape being selected)
7. Check logo section → confirm it shows the Pro-locked state (not the drop-zone)

**Test 3 — Pro user full flow (sign in with a Pro account):**
1. Sign in at http://localhost:4321/login with a Pro account
2. Visit http://localhost:4321/ — confirm all features are unlocked (no locks, Save button active)
3. Generate a QR code (enter a URL), click "Save to Library"
4. Confirm: modal opens with the URL pre-filled as the default name
5. Edit the name to "My Test QR", click Save
6. Confirm: success toast "Saved to library" appears
7. Navigate to http://localhost:4321/dashboard
8. Confirm: the saved QR appears as a card with the name "My Test QR", thumbnail, date, and URL
9. Click "Edit" on the card → confirm navigation to /?edit=[id] and edit banner shows "Editing: My Test QR"
10. Change the URL content, click "Save Changes" → confirm toast and return to dashboard
11. On the card, click "Delete" → confirm inline confirmation appears → click "Yes, delete" → confirm card disappears

**Test 4 — Full suite:**
Run: `npm run test`
Confirm: exits 0, no regressions to existing Phase 7 + Phase 8 tests
  </how-to-verify>
  <resume-signal>Type "approved" when all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run test:smoke` exits 0 — all @smoke tests pass
2. `npm run test` exits 0 — full suite green, no regressions
3. Human checkpoint approved for all 4 verification scenarios
</verification>

<success_criteria>
- All 7 Phase 9 requirements verified: LIB-01, LIB-02, LIB-03, LIB-04, GATE-01, GATE-02, GATE-03
- Full Playwright suite green
- Human sign-off on Pro/non-Pro/anonymous UX flows
- Phase 9 complete, ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/09-saved-qr-library-pro-gates/09-05-SUMMARY.md`
</output>
