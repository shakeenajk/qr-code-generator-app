---
phase: 06-fix-ghost-placeholder
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - SEO-09

must_haves:
  truths:
    - "Lighthouse mobile performance score is confirmed ≥ 90 by human running the audit"
    - "REQUIREMENTS.md SEO-09 checkbox is marked [x]"
    - "PREV-03 checkbox in REQUIREMENTS.md is marked [x]"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEO-09 and PREV-03 marked complete"
      contains: "[x] **SEO-09**"
  key_links:
    - from: "Human Lighthouse audit"
      to: "REQUIREMENTS.md SEO-09"
      via: "Manual attestation → checkbox update"
      pattern: "SEO-09"
---

<objective>
Attest Lighthouse mobile performance ≥ 90 (SEO-09) via human audit, then mark SEO-09 and PREV-03 complete in REQUIREMENTS.md.

Purpose: SEO-09 is a milestone attestation — client:visible hydration deferral was applied in Phase 4. This plan confirms the score is met and closes the last two open requirements from the v1.0 audit.
Output: Human-verified Lighthouse score recorded, REQUIREMENTS.md fully green for all v1 requirements.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-fix-ghost-placeholder/06-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Lighthouse mobile attestation — human audit required</name>
  <action>Run production Lighthouse mobile audit and visually verify the PREV-03 placeholder fix on WiFi/vCard tabs. No automated command can substitute for this — Lighthouse requires a real browser session.</action>
  <what-built>
Phase 6 code fix is complete: WiFi and vCard tabs now show the ghost placeholder when all fields are blank (PREV-03). All @smoke Playwright tests pass.

Now need human attestation of Lighthouse mobile performance score for SEO-09.
  </what-built>
  <how-to-verify>
Run a production Lighthouse mobile audit:

1. Build and preview the production bundle:
   ```
   npm run build && npm run preview
   ```
   (Server starts at http://localhost:4321)

2. Open http://localhost:4321 in Chrome

3. Open DevTools (F12) → Lighthouse tab → select "Mobile" → click "Analyze page load"

4. Wait for the report to complete (~30–60 seconds)

5. Record the Performance score (the large number in the top left of the report)

Expected: Performance score >= 90

If score is below 90: report the score and any flagged opportunities (LCP, TBT, CLS) — do not continue until investigated.

Also spot-check PREV-03 fix visually:
- Switch to WiFi tab with no input → ghost placeholder should be visible immediately
- Switch to vCard tab with no input → ghost placeholder should be visible immediately
  </how-to-verify>
  <verify>
    <automated>MISSING — this task is a human checkpoint; no automated command can run a Lighthouse mobile audit</automated>
  </verify>
  <done>Human has confirmed Lighthouse mobile performance score >= 90 and visually verified ghost placeholder on WiFi and vCard tabs.</done>
  <resume-signal>Type "approved: [score]" (e.g., "approved: 94") or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Mark SEO-09 and PREV-03 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update two lines in .planning/REQUIREMENTS.md:

1. Change `- [ ] **PREV-03**` to `- [x] **PREV-03**`
   (Line currently reads: `- [ ] **PREV-03**: Preview shows an empty/placeholder state when no content is entered`)

2. Change `- [ ] **SEO-09**` to `- [x] **SEO-09**`
   (Line currently reads: `- [ ] **SEO-09**: Page achieves Lighthouse performance score 90+ on mobile`)

Also update the Traceability table at the bottom:
- Change `| PREV-03 | Phase 6 | Pending |` to `| PREV-03 | Phase 6 | Complete |`
- Change `| SEO-09 | Phase 6 | Pending |` to `| SEO-09 | Phase 6 | Complete |`

Update the "Last updated" line at the bottom of the file to reflect today's date and completion of Phase 6.

No other changes to REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -c "\[x\]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md shows [x] for PREV-03 and SEO-09. All 36 v1 requirements are marked [x]. Traceability table shows Complete for both.</done>
</task>

</tasks>

<verification>
After Task 2:
`grep "PREV-03\|SEO-09" .planning/REQUIREMENTS.md`
Expected output shows `[x]` for both entries.

Full suite final check:
`npx playwright test`
Expected: all tests green.
</verification>

<success_criteria>
- Human confirmed Lighthouse mobile score >= 90 on production build
- REQUIREMENTS.md PREV-03 checkbox is [x]
- REQUIREMENTS.md SEO-09 checkbox is [x]
- All 36 v1 requirements are complete [x]
- Full Playwright test suite is green
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-ghost-placeholder/06-03-SUMMARY.md`
</output>
