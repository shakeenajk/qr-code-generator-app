---
phase: 12-foundation-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Header.astro
  - src/components/Hero.astro
  - src/data/faq.ts
  - src/pages/pricing.astro
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
  - TIER-01
  - TIER-02
  - TIER-04

must_haves:
  truths:
    - "Header shows a 'Pricing' navigation link between the logo and the auth buttons"
    - "Header shows a 'Sign Up' outlined-blue button next to the filled 'Sign In' button when signed out"
    - "Hero subtext no longer contains 'no limits'"
    - "FAQ answers contain no 'no signup or account' absolute claims without qualification"
    - "Pricing page Pro card shows '250 QR codes' not 'Unlimited QR codes'"
    - "Pricing page Pro card shows '100 dynamic QR codes'"
    - "Pricing page Starter card shows '10 dynamic QR codes'"
    - "Pricing page Starter card has no 'No ads' list item"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Header with Pricing link and Register button"
      contains: "Sign Up"
    - path: "src/components/Hero.astro"
      provides: "Hero with corrected subtext"
      contains: "No signup required"
    - path: "src/data/faq.ts"
      provides: "FAQ data with accurate freemium copy"
    - path: "src/pages/pricing.astro"
      provides: "Pricing page with accurate tier limits and no 'No ads'"
      contains: "250 QR codes"
  key_links:
    - from: "src/components/Header.astro"
      to: "/signup"
      via: "href on Register button"
      pattern: "href=\"/signup\""
    - from: "src/components/Header.astro"
      to: "/pricing"
      via: "href on Pricing nav link"
      pattern: "href=\"/pricing\""
---

<objective>
Fix inaccurate copy on the homepage and update Header and Pricing page to surface key conversion entry points and accurate tier limits.

Purpose: The site currently claims "no limits" in the hero, omits a Register button from the header, has no Pricing nav link, and shows "Unlimited QR codes" for Pro and missing dynamic QR counts for Starter and Pro. All these issues mislead users and suppress conversion.

Output: Updated Header.astro (Pricing link + Sign Up button), Hero.astro (corrected subtext), faq.ts (accurate answers), pricing.astro (correct tier limits for all cards, no "No ads").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-foundation-improvements/12-UI-SPEC.md
</context>

<interfaces>
<!-- Key UI contracts extracted from codebase for executor reference -->

From src/components/Header.astro (current state):
```astro
<div class="flex items-center gap-3">
  <Show when="signed-out">
    <a href="/login"
       class="bg-[#2563EB] text-white text-sm font-semibold px-4 py-2 rounded-lg hover:bg-blue-700 transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
      Sign In
    </a>
  </Show>
  <Show when="signed-in">
    <UserMenu client:only="react" />
  </Show>
</div>
```

From src/components/Hero.astro (inaccurate line to fix):
```
No signup, no limits — download as PNG or SVG instantly.
```

From src/pages/pricing.astro (items to change):
- Pro card: `Unlimited QR codes` → `250 QR codes`
- Pro card: add `100 dynamic QR codes` list item
- Starter card: missing `10 dynamic QR codes` list item
- Starter card: has `No ads` list item → remove
- Pro card: no `No ads` item present (confirmed — nothing to remove)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Header — add Pricing link and Sign Up button</name>
  <files>src/components/Header.astro</files>
  <read_first>
    - src/components/Header.astro (READ BEFORE EDITING — understand full current markup)
    - .planning/phases/12-foundation-improvements/12-UI-SPEC.md (UI contract for exact styles)
  </read_first>
  <action>
Edit `src/components/Header.astro`. Make two additions inside the signed-out Show block and one addition to the nav.

**1. Add Pricing nav link** — place between the logo `<a>` and the `<div class="flex items-center gap-3">`. Insert as a sibling at the same nav level:

```astro
<a
  href="/pricing"
  class="text-sm font-normal text-gray-700 hover:text-gray-900 dark:text-slate-300 dark:hover:text-white transition-colors"
>
  Pricing
</a>
```

**2. Add "Sign Up" button** — inside the `<Show when="signed-out">` block, insert BEFORE the existing Sign In `<a>` tag:

```astro
<a
  href="/signup"
  class="border border-[#2563EB] text-[#2563EB] text-sm font-bold px-4 py-2 rounded-lg hover:bg-blue-50 dark:border-blue-400 dark:text-blue-400 dark:hover:bg-slate-800 transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
>
  Sign Up
</a>
```

The Sign In button keeps its existing classes unchanged. Order in signed-out block: Sign Up → Sign In.

The final nav layout (left to right): Logo | Pricing link (spacer between) | Sign Up button | Sign In button (or UserMenu when signed in).

Per UI-SPEC: Pricing link uses text link style (no fill, no border). Sign Up uses outlined blue variant. Sign In retains filled blue variant.
  </action>
  <verify>
    <automated>grep -c "href=\"/signup\"" /Users/ranjit/Documents/Development/qr-generator/src/components/Header.astro && grep -c "href=\"/pricing\"" /Users/ranjit/Documents/Development/qr-generator/src/components/Header.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep "href=\"/signup\"" src/components/Header.astro` matches
    - `grep "href=\"/pricing\"" src/components/Header.astro` matches
    - `grep "Sign Up" src/components/Header.astro` matches
    - `grep "Pricing" src/components/Header.astro` matches
    - `grep "border border-\[#2563EB\]" src/components/Header.astro` matches (outlined style on Sign Up)
    - `grep "bg-\[#2563EB\]" src/components/Header.astro` matches (Sign In retains filled style)
    - Sign Up `<a>` appears before Sign In `<a>` in the file (line number of Sign Up < line number of Sign In)
    - `grep "dark:border-blue-400" src/components/Header.astro` matches (dark mode on Sign Up)
  </acceptance_criteria>
  <done>Header has Pricing text link and Sign Up outlined button; both are inside correct Clerk Show blocks; Sign In button unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Fix Hero copy and FAQ accuracy</name>
  <files>src/components/Hero.astro, src/data/faq.ts</files>
  <read_first>
    - src/components/Hero.astro (READ BEFORE EDITING)
    - src/data/faq.ts (READ BEFORE EDITING — see all 6 FAQ items)
    - .planning/phases/12-foundation-improvements/12-UI-SPEC.md (copywriting contract)
  </read_first>
  <action>
**In src/components/Hero.astro:**

Find the paragraph containing `"No signup, no limits — download as PNG or SVG instantly."` and replace it with:

```
No signup required — download as PNG or SVG instantly.
```

The surrounding `<p>` tag and all its classes remain unchanged. Only the text content changes.

**In src/data/faq.ts:**

Review all 6 FAQ items. The item most at risk of inaccuracy is:

```
question: 'Do I need to create an account to use QRCraft?',
answer: 'No. QRCraft is fully free and requires no signup or account. Generate, customize, and download QR codes instantly.',
```

This answer is accurate for anonymous static QR generation — no change needed.

Review the item:
```
question: 'How do I create a QR code for free?'
```
This answer does not claim no limits — no change needed.

No existing FAQ item contains "no limits", "unlimited", or "generate as many" phrasing. Confirm this by reading the file. If no other inaccurate claims are found, no further FAQ changes are required.

**If any FAQ answer does contain "unlimited" or "no limits" phrasing** (e.g. "generate unlimited QR codes"), update that answer to add the qualification: `"Free accounts can save up to 5 QR codes. Upgrade to Pro for higher limits."` appended to or replacing the inaccurate claim.
  </action>
  <verify>
    <automated>grep -c "no limits" /Users/ranjit/Documents/Development/qr-generator/src/components/Hero.astro && grep "No signup required" /Users/ranjit/Documents/Development/qr-generator/src/components/Hero.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep "no limits" src/components/Hero.astro` returns 0 matches
    - `grep "No signup required" src/components/Hero.astro` returns 1 match
    - `grep "no limits" src/data/faq.ts` returns 0 matches (or 0 unqualified occurrences)
    - `grep "unlimited" src/data/faq.ts` returns 0 unqualified occurrences (or any "unlimited" is accompanied by a tier qualification)
    - Hero paragraph tag structure and classes are unchanged
  </acceptance_criteria>
  <done>Hero no longer claims "no limits"; FAQ contains no absolute "unlimited" claims without tier qualification</done>
</task>

<task type="auto">
  <name>Task 3: Fix pricing page — accurate tier limits and remove No ads</name>
  <files>src/pages/pricing.astro</files>
  <read_first>
    - src/pages/pricing.astro (READ BEFORE EDITING — understand full card HTML structure)
    - .planning/phases/12-foundation-improvements/12-UI-SPEC.md (pricing section spec)
  </read_first>
  <action>
Edit `src/pages/pricing.astro`. Make the following surgical content edits. All grid layout, card styling, toggle behavior, and checkout script remain unchanged.

**1. Pro card — replace "Unlimited QR codes" with "250 QR codes" (TIER-02):**

Find the `<li>` inside the Pro card `<ul>` that contains:
```
Unlimited QR codes
```
Replace the text content with:
```
250 QR codes
```
The `<li>` structure (SVG checkmark + text) stays identical. Only the text changes.

**2. Pro card — add "100 dynamic QR codes" list item (TIER-01):**

In the Pro card `<ul>`, add a new `<li>` after the "250 QR codes" item:
```astro
<li class="flex items-center gap-2">
  <svg class="w-4 h-4 text-green-500 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
  100 dynamic QR codes
</li>
```

**3. Starter card — add "10 dynamic QR codes" list item (TIER-01):**

In the Starter card `<ul>`, add a new `<li>` after the "100 QR codes" item:
```astro
<li class="flex items-center gap-2">
  <svg class="w-4 h-4 text-green-500 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
  10 dynamic QR codes
</li>
```

**4. Starter card — remove "No ads" list item (TIER-04):**

Find the `<li>` inside the Starter card `<ul>` that contains:
```
No ads
```
Delete the entire `<li>` element (SVG + text). This is the item with "No ads" text.

**5. Pro card — confirm no "No ads" item exists:**

Read the Pro card `<ul>` content. If a "No ads" `<li>` exists, remove it. If it does not exist, no action needed.

**Note on Free card and dynamic QRs:** Per UI-SPEC decision, omit the dynamic QR row from the Free card (Free users cannot use dynamic QRs). No change to the Free card.
  </action>
  <verify>
    <automated>grep -c "Unlimited QR codes" /Users/ranjit/Documents/Development/qr-generator/src/pages/pricing.astro && grep -c "250 QR codes" /Users/ranjit/Documents/Development/qr-generator/src/pages/pricing.astro && grep -c "10 dynamic QR codes" /Users/ranjit/Documents/Development/qr-generator/src/pages/pricing.astro && grep -c "100 dynamic QR codes" /Users/ranjit/Documents/Development/qr-generator/src/pages/pricing.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Unlimited QR codes" src/pages/pricing.astro` returns 0 matches
    - `grep "250 QR codes" src/pages/pricing.astro` returns 1 match (in Pro card)
    - `grep "100 dynamic QR codes" src/pages/pricing.astro` returns 1 match (in Pro card)
    - `grep "10 dynamic QR codes" src/pages/pricing.astro` returns 1 match (in Starter card)
    - `grep "No ads" src/pages/pricing.astro` returns 0 matches
    - `grep "100 QR codes" src/pages/pricing.astro` returns 1 match (Starter card unchanged)
    - `grep "5 QR codes" src/pages/pricing.astro` returns 1 match (Free card unchanged)
    - Billing toggle script and checkout handlers are unchanged (grep for `startCheckout` still returns a match)
  </acceptance_criteria>
  <done>Pricing page shows 250 QR codes and 100 dynamic QR codes for Pro, 10 dynamic QR codes for Starter, no "No ads" items on any card</done>
</task>

</tasks>

<verification>
Visual check: run `npm run dev` in the project root, visit http://localhost:4321 and http://localhost:4321/pricing. Confirm:
1. Header shows "Pricing" text link, "Sign Up" outlined button, "Sign In" filled button — in that order
2. Hero text reads "No signup required — download as PNG or SVG instantly."
3. Pricing Pro card shows "250 QR codes"
4. Pricing Pro card shows "100 dynamic QR codes"
5. Pricing Starter card shows "10 dynamic QR codes"
6. No "No ads" item on Starter or Pro card
</verification>

<success_criteria>
- Header: Pricing link navigates to /pricing; Sign Up button navigates to /signup with outlined style
- Hero: "no limits" claim removed; "No signup required" text present
- FAQ: no unqualified "unlimited" or "no limits" claims
- Pricing: Pro shows 250 QR codes and 100 dynamic QR codes; Starter shows 10 dynamic QR codes; No ads removed from Starter
</success_criteria>

<output>
After completion, create `.planning/phases/12-foundation-improvements/12-02-SUMMARY.md`
</output>
