---
phase: 16-google-adsense
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .lighthouserc.json
  - public/ads.txt
  - src/styles/global.css
  - package.json
autonomous: true
requirements:
  - ADS-01
must_haves:
  truths:
    - "Lighthouse CI baseline score is captured and recorded before any AdSense code exists"
    - "ads.txt is accessible at the domain root and contains the correct Google authorization format"
    - "Unfilled AdSense slot CSS rule exists in global styles"
  artifacts:
    - path: ".lighthouserc.json"
      provides: "Lighthouse CI configuration with mobile performance >= 0.9 assertion"
      contains: "categories:performance"
    - path: "public/ads.txt"
      provides: "AdSense inventory authorization file"
      contains: "google.com"
    - path: "src/styles/global.css"
      provides: "CLS prevention rule for unfilled AdSense slots"
      contains: "adsbygoogle"
  key_links:
    - from: ".lighthouserc.json"
      to: "npx lhci autorun"
      via: "Lighthouse CI CLI reads config"
      pattern: "minScore.*0\\.9"
user_setup:
  - service: google-adsense
    why: "Ad serving on free tier"
    env_vars:
      - name: PUBLIC_ADSENSE_CLIENT
        source: "Google AdSense Dashboard -> Account -> Publisher ID (format: ca-pub-XXXXXXXXXXXXXXXX)"
      - name: PUBLIC_ADSENSE_SLOT
        source: "Google AdSense Dashboard -> Ads -> By ad unit -> Create display ad -> Copy slot ID"
    dashboard_config:
      - task: "Apply for AdSense account approval for qr-code-generator-app.com"
        location: "Google AdSense Dashboard -> Sites -> Add site"
      - task: "Create a horizontal display ad unit"
        location: "Google AdSense Dashboard -> Ads -> By ad unit -> Display ads"
---

<objective>
Install dependencies, create Lighthouse CI config, capture baseline performance score, create ads.txt, and add CLS prevention CSS. No AdSense code yet — this plan establishes the performance safety net and static infrastructure.

Purpose: The Lighthouse baseline must be captured BEFORE any AdSense code ships (per STATE.md). This plan creates the regression gate and static files that don't depend on the AdUnit component.
Output: `.lighthouserc.json`, `public/ads.txt`, updated `global.css`, baseline Lighthouse score recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-google-adsense/16-CONTEXT.md
@.planning/phases/16-google-adsense/16-RESEARCH.md
@.planning/phases/16-google-adsense/16-UI-SPEC.md

@src/styles/global.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create Lighthouse CI config + ads.txt</name>
  <files>package.json, .lighthouserc.json, public/ads.txt, src/styles/global.css</files>
  <read_first>
    - package.json
    - src/styles/global.css
    - .planning/phases/16-google-adsense/16-RESEARCH.md (Pattern 2: ads.txt, Pattern 3: CLS, Pattern 4: LHCI config)
  </read_first>
  <action>
1. Install dependencies:
   ```bash
   npm install @ctrl/react-adsense
   npm install --save-dev @lhci/cli
   ```

2. Create `.lighthouserc.json` in project root with this exact content:
   ```json
   {
     "ci": {
       "collect": {
         "url": ["http://localhost:4323/"],
         "numberOfRuns": 3,
         "settings": {
           "formFactor": "mobile",
           "throttling": {
             "rttMs": 40,
             "throughputKbps": 10240,
             "cpuSlowdownMultiplier": 4
           }
         }
       },
       "assert": {
         "assertions": {
           "categories:performance": ["error", { "minScore": 0.9 }]
         }
       }
     }
   }
   ```

3. Create `public/ads.txt` with placeholder publisher ID:
   ```
   google.com, pub-XXXXXXXXXXXXXXXX, DIRECT, f08c47fec0942fa0
   ```
   The `f08c47fec0942fa0` is Google's fixed TAG ID — it never changes. The `pub-XXXXXXXXXXXXXXXX` placeholder will be replaced with the real publisher ID when the user provides it.

4. Add CLS prevention rule to `src/styles/global.css` AFTER the existing `@layer base` block:
   ```css
   /* Prevent CLS when AdSense returns no ad */
   ins.adsbygoogle[data-ad-status="unfilled"] {
     display: none !important;
   }
   ```
  </action>
  <verify>
    <automated>node -e "const pkg=require('./package.json'); const deps={...pkg.dependencies,...pkg.devDependencies}; console.assert(deps['@ctrl/react-adsense'],'missing @ctrl/react-adsense'); console.assert(deps['@lhci/cli'],'missing @lhci/cli'); console.log('deps OK');" && test -f .lighthouserc.json && test -f public/ads.txt && grep -q 'adsbygoogle' src/styles/global.css && echo "All files OK"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `@ctrl/react-adsense` in dependencies
    - `package.json` lists `@lhci/cli` in devDependencies
    - `.lighthouserc.json` exists with `minScore: 0.9` assertion for `categories:performance`
    - `public/ads.txt` exists with `google.com, pub-XXXXXXXXXXXXXXXX, DIRECT, f08c47fec0942fa0`
    - `src/styles/global.css` contains `ins.adsbygoogle[data-ad-status="unfilled"]` rule
  </acceptance_criteria>
  <done>Dependencies installed, LHCI config created with mobile perf >= 90 gate, ads.txt created with placeholder publisher ID, CLS prevention CSS added to global styles</done>
</task>

<task type="auto">
  <name>Task 2: Capture Lighthouse CI baseline (pre-AdSense)</name>
  <files>.lighthouserc.json</files>
  <read_first>
    - .lighthouserc.json
    - .planning/phases/16-google-adsense/16-RESEARCH.md (Pattern 4: Lighthouse CI Baseline Gate)
  </read_first>
  <action>
1. Run the Lighthouse CI baseline measurement against the dev server (no AdSense code exists yet):
   ```bash
   npx lhci autorun --collect.startServerCommand="npm run preview -- --port 4323" --collect.startServerReadyPattern="localhost"
   ```
   If the dev server command differs, check `package.json` scripts and adjust. The key is to run against a built version of the site. Try `npm run build` first, then serve the built output.

   Alternative if `preview` script is unavailable:
   ```bash
   npm run build && npx lhci autorun --collect.staticDistDir=./dist
   ```

2. Record the baseline score in the SUMMARY. The score MUST be >= 90 (if it isn't, there's a pre-existing performance issue to investigate before proceeding).

3. If LHCI reports errors about Chrome, try with `--collect.chromePath` pointing to the system Chrome installation, or install `puppeteer` as a fallback.

NOTE: If Lighthouse CI cannot run in this environment (e.g., no headed Chrome available), document the limitation and verify manually with `npx lighthouse http://localhost:4321 --output=json --chrome-flags="--headless"` instead. The critical output is the mobile performance score number.
  </action>
  <verify>
    <automated>echo "Baseline captured — score recorded in task output. Manual verification: check SUMMARY for baseline score >= 90."</automated>
  </verify>
  <acceptance_criteria>
    - Lighthouse mobile performance baseline score is captured and documented
    - Score is >= 90 (confirming no pre-existing regression)
    - Baseline was measured BEFORE any AdSense component code exists in the codebase
  </acceptance_criteria>
  <done>Lighthouse CI baseline captured pre-AdSense, score documented, performance safety net established for Plan 02 comparison</done>
</task>

</tasks>

<verification>
- `.lighthouserc.json` exists and contains mobile performance >= 0.9 assertion
- `public/ads.txt` exists and contains Google authorization line
- `src/styles/global.css` contains `ins.adsbygoogle[data-ad-status="unfilled"]` CLS rule
- `@ctrl/react-adsense` and `@lhci/cli` are in package.json
- Lighthouse baseline score is captured and >= 90
</verification>

<success_criteria>
Performance safety net is in place: LHCI config, baseline score, and static infrastructure files all exist before any AdSense component code is written. Plan 02 can proceed with AdUnit implementation knowing the baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/16-google-adsense/16-01-SUMMARY.md`
</output>
