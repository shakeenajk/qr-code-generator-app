---
phase: 04-export-and-launch
plan: 03
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/layouts/Layout.astro
  - src/components/Header.astro
  - src/components/Hero.astro
  - src/components/Footer.astro
  - src/components/QRGeneratorIsland.tsx
autonomous: true
requirements:
  - BRAND-04

must_haves:
  truths:
    - "Page body background is #0f172a (slate-900) when OS dark mode is active"
    - "Header background and border change to dark variants in dark mode"
    - "Hero section background and text change to dark variants in dark mode"
    - "Footer background and border change to dark variants in dark mode"
    - "QR generator panel (bg-gray-50 container in Hero.astro) has a dark variant"
    - "Form inputs, tab bar, borders, and customize panel within QRGeneratorIsland change to dark variants"
    - "QR preview container (QRPreview.tsx bg-white) does NOT change — stays white in dark mode"
    - "No JavaScript flicker on page load — dark: media classes applied before first paint"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "dark:bg-[#0f172a] dark:text-slate-100 on body"
    - path: "src/components/Header.astro"
      provides: "dark:bg-slate-900 dark:border-slate-700 on header, dark:text-white on brand name"
    - path: "src/components/Hero.astro"
      provides: "dark:bg-slate-900 on section, dark:bg-slate-800 on generator panel, dark:border-slate-700 on panel border"
    - path: "src/components/Footer.astro"
      provides: "dark:bg-slate-900 dark:border-slate-700 on footer, dark:text-slate-400 on text"
    - path: "src/components/QRGeneratorIsland.tsx"
      provides: "dark: classes on tab bar, customize section border, tab buttons, h2 heading, form panel"
  key_links:
    - from: "src/layouts/Layout.astro body"
      to: "CSS prefers-color-scheme: dark"
      via: "Tailwind v4 dark: prefix (zero config required)"
      pattern: "dark:bg-\\[#0f172a\\]"
    - from: "QRPreview.tsx"
      to: "(no dark: classes)"
      via: "Locked decision: QR preview container MUST stay white bg-white — do NOT add dark: override"
      pattern: "bg-white"
---

<objective>
Add Tailwind v4 dark mode to all site chrome components. Page background, nav, panels, text, borders, and form elements switch to dark variants when OS dark mode is active. QR preview container explicitly stays white per locked decision.

Purpose: Make QRCraft feel complete and polished in dark mode — not an afterthought.
Output: Five modified files with dark: utility classes. Zero new dependencies.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-export-and-launch/04-CONTEXT.md
@.planning/phases/04-export-and-launch/04-RESEARCH.md
@.planning/phases/04-export-and-launch/04-01-SUMMARY.md

<interfaces>
<!-- Current classes on elements that need dark: variants. Extracted from live source. -->

Layout.astro body:
  class="bg-white text-gray-900 antialiased"
  → add: dark:bg-[#0f172a] dark:text-slate-100

Header.astro:
  <header class="bg-white border-b border-gray-200 sticky top-0 z-50">
  → add: dark:bg-slate-900 dark:border-slate-700
  <span class="text-xl font-bold text-gray-900 tracking-tight">QRCraft</span>
  → add: dark:text-white
  CTA button is already brand-colored — no dark: change needed

Hero.astro:
  <section class="bg-white py-16 sm:py-24">
  → add: dark:bg-[#0f172a]
  <h1 class="... text-gray-900 ...">
  → add: dark:text-white
  <p class="... text-gray-600 ...">
  → add: dark:text-slate-300
  <div id="qr-generator-root" class="bg-gray-50 rounded-xl border border-gray-200 p-6 text-left">
  → add: dark:bg-slate-800 dark:border-slate-700

Footer.astro:
  <footer class="bg-gray-50 border-t border-gray-200 mt-16">
  → add: dark:bg-slate-900 dark:border-slate-700
  <p class="text-sm text-gray-500">
  → add: dark:text-slate-400
  nav links: class="... text-gray-500"
  → add: dark:text-slate-400, hover: dark:hover:text-slate-200

QRGeneratorIsland.tsx (Claude's discretion — apply dark: to chrome, NOT to QRPreview):
  Tab bar border: border-gray-200
  → add: dark:border-slate-700
  Inactive tab button text-gray-500: → add: dark:text-slate-400
  Inactive tab hover border-gray-300: → add: dark:hover:border-slate-500
  Customize section border-t border-gray-200: → add: dark:border-slate-700
  Customize h2 text-gray-900: → add: dark:text-white
  Form panel bg: the panel itself is bg-gray-50 in Hero.astro (already handled above)

CRITICAL: Do NOT add any dark: class to QRPreview.tsx or the inner preview div.
The bg-white on the QRPreview container div must remain unchanged.
This is a LOCKED DECISION — the QR code must stay on a white background.

Tailwind v4: No config change needed. @import "tailwindcss" in global.css already enables dark: via prefers-color-scheme. Just add dark: utility classes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark: classes to Astro layout shell (Layout, Header, Hero, Footer)</name>
  <files>src/layouts/Layout.astro, src/components/Header.astro, src/components/Hero.astro, src/components/Footer.astro</files>
  <action>
    Modify four Astro files to add dark: utility classes. This is a mechanical class addition — no structural changes.

    Layout.astro — body tag:
    ```astro
    <body class="bg-white text-gray-900 antialiased dark:bg-[#0f172a] dark:text-slate-100">
    ```

    Header.astro — header element and brand span:
    ```astro
    <header class="bg-white border-b border-gray-200 sticky top-0 z-50 dark:bg-slate-900 dark:border-slate-700">
    ```
    ```astro
    <span class="text-xl font-bold text-gray-900 tracking-tight dark:text-white">QRCraft</span>
    ```

    Hero.astro — section, h1, p, and generator root div:
    ```astro
    <section class="bg-white py-16 sm:py-24 dark:bg-[#0f172a]">
    ```
    h1: add dark:text-white
    p (subtitle): add dark:text-slate-300
    ```astro
    <div id="qr-generator-root" class="bg-gray-50 rounded-xl border border-gray-200 p-6 text-left dark:bg-slate-800 dark:border-slate-700">
    ```

    Footer.astro — footer element, copyright text, nav links:
    ```astro
    <footer class="bg-gray-50 border-t border-gray-200 mt-16 dark:bg-slate-900 dark:border-slate-700">
    ```
    Copyright p: add dark:text-slate-400
    Nav links: add dark:text-slate-400 dark:hover:text-slate-200

    Do NOT touch QRPreview.tsx. Do NOT add dark: to the preview container or its children.
  </action>
  <verify>
    <automated>npm run build && npx playwright test --grep "@smoke.*dark" tests/export.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Body background computes as rgb(15, 23, 42) in dark mode context. Header and footer have dark backgrounds. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add dark: classes to QRGeneratorIsland chrome elements</name>
  <files>src/components/QRGeneratorIsland.tsx</files>
  <action>
    Modify QRGeneratorIsland.tsx to add dark: classes to the tab bar, customize section divider, and headings. Do NOT touch QRPreview rendering.

    Tab bar div (role="tablist"):
    - border-b border-gray-200 → add dark:border-slate-700

    Inactive tab buttons (the `border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300` class string):
    - add dark:text-slate-400 dark:hover:text-gray-200 dark:hover:border-slate-500

    Customize section border div (className="mt-8 border-t border-gray-200 pt-6 space-y-6"):
    - add dark:border-slate-700

    Customize h2 (text-gray-900):
    - add dark:text-white

    NOTE: Form inputs within UrlTab, TextTab, WifiTab, VCardTab are in separate tab component files. If those inputs are visibly broken in dark mode (white text on white bg), add dark: to them too — check src/components/tabs/*.tsx and add dark:bg-slate-800 dark:text-slate-100 dark:border-slate-600 dark:placeholder-slate-400 to input/textarea elements as needed. This is Claude's discretion for secondary surface tokens.

    IMPORTANT: Do NOT modify QRPreview.tsx. The preview div bg-white is intentionally excluded from dark mode.
  </action>
  <verify>
    <automated>npm run build && npx playwright test --grep @smoke tests/export.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <done>All BRAND-04 dark mode smoke tests pass. Tab bar and customize section are readable in dark mode. Build exits 0.</done>
</task>

</tasks>

<verification>
Full smoke suite for dark mode:
`npm run build && npx playwright test --grep @smoke tests/export.spec.ts`

BRAND-04 tests must pass:
- Body bg is rgb(15, 23, 42) in dark mode
- Header bg is not white in dark mode
- QR preview container bg remains white in dark mode
</verification>

<success_criteria>
1. All five files modified with dark: utility classes.
2. No structural changes — only class additions.
3. QRPreview.tsx untouched (bg-white preserved).
4. BRAND-04 smoke tests green.
5. TypeScript build passes (no type errors from class changes).
6. Full smoke suite remains green (no regressions).
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-launch/04-03-SUMMARY.md`
</output>
