---
phase: 04-export-and-launch
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/export.spec.ts
autonomous: true
requirements:
  - EXPO-01
  - EXPO-02
  - EXPO-03
  - EXPO-04
  - BRAND-04

must_haves:
  truths:
    - "Smoke test file exists and all tests are in RED (failing) state"
    - "Every export requirement (EXPO-01 through EXPO-04) has at least one failing test"
    - "BRAND-04 dark mode has at least one failing test"
    - "Test commands run without Playwright config errors"
  artifacts:
    - path: "tests/export.spec.ts"
      provides: "Wave 0 failing smoke stubs for all export + dark mode requirements"
      exports: []
  key_links:
    - from: "tests/export.spec.ts"
      to: "src/components/ExportButtons.tsx"
      via: "data-testid selectors (export-png, export-svg, export-copy)"
      pattern: "data-testid=\"export-(png|svg|copy)\""
---

<objective>
Write failing Playwright smoke tests (Wave 0 TDD stubs) for every export and dark mode requirement before any implementation code exists.

Purpose: Establish the data-testid selector contract for ExportButtons.tsx and dark mode so implementation tasks have a clear pass/fail target.
Output: tests/export.spec.ts with failing tests covering EXPO-01, EXPO-02, EXPO-03, EXPO-04, BRAND-04.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-export-and-launch/04-CONTEXT.md
@.planning/phases/04-export-and-launch/04-RESEARCH.md
@.planning/phases/04-export-and-launch/04-VALIDATION.md
</context>

<feature>
  <name>Export and Dark Mode — Wave 0 Smoke Test Stubs</name>
  <files>tests/export.spec.ts</files>
  <behavior>
    All tests must FAIL before implementation (RED phase). Tests define the selector contract.

    Selector contract established by this file:
    - [data-testid="export-png"]   — Download PNG button
    - [data-testid="export-svg"]   — Download SVG button
    - [data-testid="export-copy"]  — Copy to Clipboard button

    Tests to write:

    EXPO-01/EXPO-02 — Download button existence and disabled/enabled state:
    - @smoke: "export-png button is visible" — page.goto('/'), expect locator('[data-testid="export-png"]').toBeVisible()
    - @smoke: "export-svg button is visible" — same pattern
    - @smoke: "export-png is disabled when no content entered" — check disabled attribute present initially
    - @smoke: "export-svg is disabled when no content entered" — same pattern
    - @smoke: "export-png is enabled after content entered" — fill url-input, wait 400ms debounce, expect NOT disabled
    - @smoke: "export-svg is enabled after content entered" — same pattern
    - @smoke: "export-png triggers download with filename containing qrcraft-code" — Promise.all([page.waitForEvent('download'), page.click('[data-testid="export-png"]')]), expect download.suggestedFilename() to contain 'qrcraft-code'
    - @smoke: "export-svg triggers download with filename containing qrcraft-code" — same pattern for svg

    EXPO-03/EXPO-04 — Copy button:
    - @smoke: "export-copy button is visible" — basic visibility
    - @smoke: "export-copy is disabled when no content entered"
    - @smoke: "export-copy is enabled after content entered"
    - @smoke: "export-copy shows Copied! on success" — grant clipboard-write permission, fill input, click copy, expect button text to be 'Copied!'
    - @smoke: "export-copy shows Copy not supported when clipboard unavailable" — addInitScript to remove navigator.clipboard, fill input, click, expect text 'Copy not supported'

    BRAND-04 — Dark mode:
    - @smoke: "body has dark background in dark mode" — newContext({ colorScheme: 'dark' }), goto('/'), evaluate body background-color, expect 'rgb(15, 23, 42)' (#0f172a = slate-900)
    - @smoke: "header has dark background in dark mode" — same context, check header computed bg matches dark theme (not white)
    - @smoke: "qr preview container keeps light background in dark mode" — same context, check [data-testid="qr-preview"] parent computed bg is white (rgb(255, 255, 255))

    All tests tagged @smoke (embedded in test name) for --grep @smoke compatibility (established Phase 1 pattern).
  </behavior>
  <implementation>
    Create tests/export.spec.ts from scratch following the exact patterns in 04-RESEARCH.md (Playwright test patterns section).

    Key implementation notes:
    - Use `import { test, expect, browser } from '@playwright/test'` — browser fixture for context-level tests
    - The file needs a production build to test against. The playwright.config.ts webServer is `npm run preview` on port 4321 — same as prior phases.
    - For download tests, use `Promise.all([page.waitForEvent('download'), page.click(...)])` pattern.
    - For clipboard EXPO-04 test, use `page.addInitScript(() => { Object.defineProperty(navigator, 'clipboard', { value: undefined, configurable: true }); })` BEFORE page.goto().
    - For BRAND-04 context tests, use `browser.newContext({ colorScheme: 'dark' })` and close the context after the test.
    - ALL tests should fail in RED state since ExportButtons.tsx does not exist yet.

    IMPORTANT: The `tests/` directory already has smoke.spec.ts and customization.spec.ts — place export.spec.ts alongside them.
  </implementation>
</feature>

<verification>
<automated>npm run build && npx playwright test --grep @smoke tests/export.spec.ts 2>&1 | tail -20</automated>

Expected: Tests run and FAIL (RED). Zero passing tests. Error messages reference missing elements (data-testid="export-png" not found). This is correct — RED state is the goal for Wave 0.
</verification>

<success_criteria>
tests/export.spec.ts exists with 15+ test cases covering EXPO-01, EXPO-02, EXPO-03, EXPO-04, BRAND-04. All tests tagged @smoke. All tests fail in RED state. No Playwright config or syntax errors.
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-launch/04-01-SUMMARY.md`
</output>
