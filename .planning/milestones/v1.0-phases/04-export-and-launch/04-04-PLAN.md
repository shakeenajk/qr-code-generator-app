---
phase: 04-export-and-launch
plan: 04
type: execute
wave: 2
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/components/Hero.astro
autonomous: false
requirements:
  - SEO-09

must_haves:
  truths:
    - "Lighthouse mobile performance score is 90 or higher on a production build"
    - "client:visible is applied to QRGeneratorIsland in Hero.astro (defers below-fold hydration)"
    - "Human has verified the score visually via DevTools Lighthouse"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "client:visible directive on QRGeneratorIsland (was client:load)"
  key_links:
    - from: "src/components/Hero.astro"
      to: "src/components/QRGeneratorIsland.tsx"
      via: "client:visible (IntersectionObserver-deferred hydration)"
      pattern: "client:visible"
---

<objective>
Switch QRGeneratorIsland from client:load to client:visible to defer React hydration until the element enters the viewport, reducing Total Blocking Time and improving Lighthouse mobile score. Then run a production Lighthouse audit with human verification.

Purpose: SEO-09 requires Lighthouse mobile score >= 90. client:visible is the primary lever — defers the React + qr-code-styling JS bundle execution until the user scrolls to the generator.
Output: Hero.astro with client:visible. Human-verified Lighthouse score >= 90.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-export-and-launch/04-CONTEXT.md
@.planning/phases/04-export-and-launch/04-RESEARCH.md
@.planning/phases/04-export-and-launch/04-02-SUMMARY.md
@.planning/phases/04-export-and-launch/04-03-SUMMARY.md

<interfaces>
<!-- Current Hero.astro line that needs changing: -->
<!-- <QRGeneratorIsland client:load /> -->
<!-- Change to: -->
<!-- <QRGeneratorIsland client:visible /> -->

<!--
client:visible behavior (from Astro docs):
- Uses IntersectionObserver to detect when the component enters the viewport
- On mobile (generator below the fold): hydration is deferred, reducing TBT during LCP window
- On desktop (generator may be visible on load): hydrates immediately — same as client:load
- Cannot be worse than client:load in any scenario

From 04-RESEARCH.md pitfall #5:
"Switching client:load to client:visible on QRGeneratorIsland in Hero.astro defers hydration
until the element enters the viewport. On mobile, where the generator is below fold, this
directly reduces TBT and improves the Lighthouse score."
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch QRGeneratorIsland to client:visible</name>
  <files>src/components/Hero.astro</files>
  <action>
    In Hero.astro, change the single line:
      `<QRGeneratorIsland client:load />`
    to:
      `<QRGeneratorIsland client:visible />`

    That is the only change to this file.

    After making the change, run a production build and verify the full test suite still passes (export + dark mode tests must remain green — no regressions from hydration timing change).

    If any export tests fail due to hydration timing (tests clicking buttons before island mounts), add `await page.waitForSelector('[data-testid="export-png"]')` before interacting with buttons in export.spec.ts. The 400ms debounce waits should be sufficient for IntersectionObserver to fire in Playwright's Chromium.
  </action>
  <verify>
    <automated>npm run build && npm run test 2>&1 | tail -30</automated>
  </verify>
  <done>Hero.astro uses client:visible. Full test suite (all 3 spec files) passes. Build exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — exports, dark mode, and Lighthouse score</name>
  <files>src/components/Hero.astro</files>
  <action>
    Run `npm run build && npm run preview` to start the production server, then verify all three areas below.

    1. EXPORT BUTTONS (EXPO-01/02/03/04):
       - Open http://localhost:4321
       - Three ghost/outlined buttons appear below the QR preview: "Download PNG", "Download SVG", "Copy"
       - Buttons are disabled (muted) when no content is entered
       - Type "https://example.com" in the URL input — buttons become enabled after ~300ms
       - Click "Download PNG" — a qrcraft-code.png file downloads; open it and confirm it is >= 768px wide
       - Click "Download SVG" — a qrcraft-code.svg file downloads; open in text editor and confirm it has path or rect elements (not base64 data)
       - Click "Copy" — button briefly shows "Copied!" then reverts to "Copy"

    2. DARK MODE (BRAND-04):
       - Enable dark mode in System Preferences (macOS: System Settings -> Appearance -> Dark)
       - Reload http://localhost:4321
       - Confirm: page background is dark (slate-900 #0f172a), header is dark, footer is dark
       - Confirm: QR preview container (the white square) remains white — not dark
       - Confirm: Text is readable (high contrast against dark bg)
       - Confirm: No white flash on page load

    3. LIGHTHOUSE PERFORMANCE (SEO-09):
       - Open http://localhost:4321 in Chrome (incognito window, no extensions)
       - Open DevTools (F12) -> Lighthouse tab
       - Set: Mode = Navigation, Device = Mobile
       - Click "Analyze page load"
       - Confirm Performance score >= 90

    If Lighthouse score is below 90, look at the "Opportunities" section and report what is blocking the score.
  </action>
  <verify>
    <automated>npm run build && npm run test 2>&1 | tail -10</automated>
  </verify>
  <done>
    Human confirms: (1) export buttons present and functional — PNG at 3x, SVG is true vector, Copy shows Copied! feedback. (2) Dark mode looks complete — chrome is dark, QR preview stays white, no FOUC. (3) Lighthouse mobile performance >= 90.
  </done>
</task>

</tasks>

<verification>
Full suite must be green before checkpoint:
`npm run build && npm run test`

Manual Lighthouse audit >= 90 confirmed by human.
</verification>

<success_criteria>
1. Hero.astro uses client:visible (not client:load).
2. Full test suite green (smoke.spec.ts + export.spec.ts + customization.spec.ts).
3. Human confirmed: export buttons work (PNG downloads at 3x, SVG is true vector, Copy shows feedback).
4. Human confirmed: dark mode looks complete — chrome is dark, QR preview stays white.
5. Human confirmed: Lighthouse mobile performance score >= 90.
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-launch/04-04-SUMMARY.md`
</output>
