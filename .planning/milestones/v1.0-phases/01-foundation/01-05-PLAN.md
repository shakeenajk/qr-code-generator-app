---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - src/pages/index.astro
  - src/components/Hero.astro
  - src/components/Features.astro
  - src/components/FAQ.astro
autonomous: false
requirements: [BRAND-01, BRAND-02, BRAND-03, SEO-01, SEO-02, SEO-03, SEO-04, SEO-05, SEO-06, SEO-07, SEO-08]

must_haves:
  truths:
    - "The site loads and is navigable at http://localhost:4321 (dev) and after npm run build + preview"
    - "Page has exactly one h1 ('Free QR Code Generator' or similar) and multiple h2s for sections"
    - "The QRCraft SVG logo is visible in the header"
    - "A visible FAQ section renders with at least 5 Q&A items sourced from FAQ_ITEMS"
    - "A stubbed generator placeholder div exists with id='qr-generator-root'"
    - "All 11 Playwright smoke tests pass green"
    - "robots.txt and sitemap-index.xml return 200 after build"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Single page composing all section components inside Layout"
    - path: "src/components/Hero.astro"
      provides: "Hero section with h1, tagline, and generator placeholder stub"
      contains: "id=\"qr-generator-root\""
    - path: "src/components/Features.astro"
      provides: "Feature bullets section with h2"
    - path: "src/components/FAQ.astro"
      provides: "FAQ section with visible Q&A items from FAQ_ITEMS (same array as JSON-LD schema)"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/Layout.astro"
      via: "wraps all page content in <Layout title=... description=...>"
      pattern: "import Layout"
    - from: "src/components/FAQ.astro"
      to: "src/data/faq.ts"
      via: "import FAQ_ITEMS — same array as JSON-LD FAQPage schema"
      pattern: "import.*FAQ_ITEMS"
    - from: "src/components/Hero.astro"
      to: "Phase 2 generator island"
      via: "div#qr-generator-root — stable mount point"
      pattern: "qr-generator-root"
---

<objective>
Assemble the complete page: compose all components into `src/pages/index.astro`, build the Hero section with the generator placeholder stub, create the Features and FAQ sections. Run the full Playwright smoke suite — all 11 tests must be green before this plan is marked complete.

Purpose: This plan wires everything together and proves Phase 1 is complete. The smoke suite green-lighting is the acceptance gate.
Output: A fully deployable, crawlable site that passes all BRAND and SEO acceptance tests.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 03: Layout.astro accepts these props -->
<!--
  interface Props {
    title: string;
    description: string;
    ogImage?: string;
  }
-->

<!-- From Plan 03: FAQ_ITEMS from src/data/faq.ts -->
<!--
  export interface FaqItem { question: string; answer: string; }
  export const FAQ_ITEMS: FaqItem[]  — 5+ items
-->

<!-- From Plan 04: Components available -->
<!--
  src/components/Logo.astro    — accepts size?: number, class?: string
  src/components/Header.astro  — no props
  src/components/Footer.astro  — no props
-->

<!-- Generator stub shape (from RESEARCH.md Pattern 4) -->
<!--
  <div id="qr-generator-root" class="min-h-[400px] bg-gray-50 rounded-xl border border-gray-200">
    <!-- Phase 2 drops the React island here — do NOT remove this div -->
  </div>
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build page components — Hero, Features, FAQ</name>
  <files>src/components/Hero.astro, src/components/Features.astro, src/components/FAQ.astro</files>
  <action>
Create three section components. All must use semantic HTML landmarks per SEO-08. Only ONE `h1` is allowed per page (in Hero). All other section headings use `h2`.

**`src/components/Hero.astro`:**

```astro
---
// src/components/Hero.astro
---

<section class="bg-white py-16 sm:py-24">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h1 class="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-gray-900 tracking-tight mb-6">
      Free QR Code Generator
    </h1>
    <p class="text-xl text-gray-600 max-w-2xl mx-auto mb-10">
      Create custom QR codes for URLs, text, WiFi, and contact cards.
      No signup, no limits — download as PNG or SVG instantly.
    </p>

    <!-- Generator island mount point — Phase 2 drops React island here. DO NOT remove this div. -->
    <div
      id="qr-generator-root"
      class="min-h-[400px] bg-gray-50 rounded-xl border border-gray-200 flex items-center justify-center"
      aria-label="QR code generator"
    >
      <p class="text-gray-400 text-sm">QR generator loads here</p>
    </div>
  </div>
</section>
```

**`src/components/Features.astro`:**

```astro
---
// src/components/Features.astro

const features = [
  {
    icon: '🔗',
    title: 'Four Content Types',
    description: 'URLs, plain text, WiFi credentials, and vCard contacts — all supported.',
  },
  {
    icon: '🎨',
    title: 'Full Customization',
    description: 'Choose dot shapes, eye styles, colors, and gradients to match your brand.',
  },
  {
    icon: '🖼️',
    title: 'Logo Embedding',
    description: 'Upload your logo and embed it in the center of your QR code.',
  },
  {
    icon: '⬇️',
    title: 'PNG & SVG Export',
    description: 'Download print-ready PNG at 3× resolution or true vector SVG.',
  },
  {
    icon: '⚡',
    title: 'Instant Preview',
    description: 'Every change reflects live in the preview — no form submission needed.',
  },
  {
    icon: '🔒',
    title: 'No Signup Required',
    description: 'Completely free. No account, no data stored, no friction.',
  },
];
---

<section class="bg-gray-50 py-16 sm:py-20" id="features">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-gray-900 text-center mb-12">
      Everything You Need
    </h2>
    <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6" role="list">
      {features.map((f) => (
        <li class="bg-white rounded-xl border border-gray-200 p-6">
          <span class="text-3xl mb-3 block" aria-hidden="true">{f.icon}</span>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">{f.title}</h3>
          <p class="text-gray-600 text-sm leading-relaxed">{f.description}</p>
        </li>
      ))}
    </ul>
  </div>
</section>
```

Note: Feature items use `h3` inside a list — this is correct heading hierarchy because Features section uses `h2` as its section heading, and `h3` for sub-items is valid. This does NOT violate the one-h1 rule.

**`src/components/FAQ.astro`:**

This component MUST import from the same `src/data/faq.ts` array as the JSON-LD schema in Layout.astro — single source of truth (prevents Google's "schema doesn't match visible content" error).

```astro
---
// src/components/FAQ.astro
import { FAQ_ITEMS } from '../data/faq';
---

<section class="bg-white py-16 sm:py-20" id="faq" aria-labelledby="faq-heading">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 id="faq-heading" class="text-3xl font-bold text-gray-900 text-center mb-12">
      Frequently Asked Questions
    </h2>
    <dl class="space-y-6">
      {FAQ_ITEMS.map((item) => (
        <div class="border border-gray-200 rounded-xl p-6 bg-gray-50">
          <dt
            class="text-base font-semibold text-gray-900 mb-2"
            data-faq-question
          >
            {item.question}
          </dt>
          <dd class="text-gray-600 text-sm leading-relaxed">
            {item.answer}
          </dd>
        </div>
      ))}
    </dl>
  </div>
</section>
```

The `data-faq-question` attribute on each `<dt>` is the selector the smoke test uses to count FAQ items (`page.locator('[data-faq-question]')`). Do NOT remove it.
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
All three components exist. `npm run build` exits 0 — no import or Astro compilation errors. Hero has `id="qr-generator-root"`, FAQ imports from `src/data/faq.ts`, Features has `h2` heading.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble index.astro page and run full smoke suite</name>
  <files>src/pages/index.astro</files>
  <action>
Create `src/pages/index.astro` — the single page that composes all section components inside the Layout.

```astro
---
// src/pages/index.astro
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import Features from '../components/Features.astro';
import FAQ from '../components/FAQ.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="Free QR Code Generator — QRCraft"
  description="Create custom QR codes for URLs, WiFi, contact cards, and text. Download as PNG or SVG instantly. Free, no signup required."
>
  <Header />
  <main>
    <Hero />
    <Features />
    <FAQ />
  </main>
  <Footer />
</Layout>
```

Title: "Free QR Code Generator — QRCraft" — targets primary keyword phrase, single h1 on page will match (SEO-01).
Description: 50+ chars, includes target keywords "QR code", "PNG", "SVG", "free", "no signup" (SEO-01).

After creating index.astro, run the full build + smoke suite:

```bash
cd /Users/ranjit/Documents/Development/qr-generator
npm run build
npx playwright test --project=chromium --grep @smoke
```

If any test fails, diagnose and fix before proceeding:
- "logo" test fails → check Logo.astro has `aria-label="QRCraft logo"` and is inside `<header>`
- "brand colors" test fails → check Layout.astro body has `bg-white`, check computed style is white
- "mobile" test fails → check no element has a fixed width wider than 375px
- "meta tags" test fails → check Layout.astro title prop is passed and rendered, description meta present
- "open graph" test fails → check og:image is an absolute URL (requires Astro.site to be set)
- "webapplication schema" test fails → check set:html on JSON-LD script, check name is 'QRCraft'
- "faqpage schema" test fails → check FAQ_ITEMS has 4+ items, set:html on second JSON-LD script
- "faq section" test fails → check FAQ component renders, `data-faq-question` attrs present, count >= 4
- "sitemap" test fails → check dist/sitemap-index.xml exists (site: config + sitemap integration)
- "robots" test fails → check public/robots.txt has User-agent: * and Sitemap: line
- "semantic html" test fails → check exactly ONE h1 on page, check main and nav landmarks present

Do NOT mark this plan complete until all 11 smoke tests are green.
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npm run build && npx playwright test --project=chromium --grep @smoke 2>&1 | tail -20</automated>
  </verify>
  <done>
`src/pages/index.astro` exists. `npm run build` exits 0. All 11 Playwright smoke tests pass green on Chromium: logo, brand colors, mobile, meta tags, open graph, webapplication schema, faqpage schema, faq section, sitemap, robots, semantic html. Reporter shows "11 passed".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual and functional verification of complete Phase 1 site</name>
  <files>— (no files modified; human verification only)</files>
  <action>Human visually verifies the complete Phase 1 site per the checklist below.</action>
  <verify><automated>npx playwright test --project=chromium --grep @smoke 2>&1 | tail -5</automated></verify>
  <done>Human approves visual and functional verification. All smoke tests are green.</done>
  <what-built>
Complete Phase 1 static site: Astro 5 project scaffolded, Tailwind v4, Layout with full SEO head (meta, OG, JSON-LD), QRCraft SVG logo, Header, Hero with generator placeholder, Features, FAQ (sourced from shared data), Footer. robots.txt and sitemap generated. All 11 Playwright smoke tests green.
  </what-built>
  <how-to-verify>
1. Run: `npm run build && npm run preview`
2. Open http://localhost:4321
3. Verify visually:
   - QRCraft logo (dot-grid "Q") visible in header, colored blue
   - "Free QR Code Generator" headline visible below nav
   - "Everything You Need" feature grid visible with 6 cards
   - "Frequently Asked Questions" section visible with 6 Q&A items
   - Footer with copyright notice at bottom
4. Right-click → View Page Source. Confirm presence of:
   - `<title>Free QR Code Generator — QRCraft</title>`
   - `<meta name="description"` with meaningful content
   - `<meta property="og:image"` with an absolute https:// URL
   - Two `<script type="application/ld+json">` blocks (WebApplication and FAQPage)
5. Check http://localhost:4321/robots.txt — should show User-agent: * and Sitemap: lines
6. Check http://localhost:4321/sitemap-index.xml — should show XML with sitemap entries
7. Open DevTools → resize to 375px width — no horizontal scroll, layout intact
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any visual/functional issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. All 11 Playwright smoke tests pass: `npx playwright test --project=chromium --grep @smoke`
2. Full suite passes: `npx playwright test`
3. Page renders correctly in browser at localhost:4321
4. View source confirms title, meta description, OG tags, both JSON-LD scripts
5. /robots.txt and /sitemap-index.xml return 200 with correct content
6. Human verification approved via checkpoint
</verification>

<success_criteria>
- `src/pages/index.astro` composes Layout + Header + main(Hero + Features + FAQ) + Footer
- One `h1` on page ("Free QR Code Generator" or equivalent), `h2` for Features and FAQ sections
- `div#qr-generator-root` exists in Hero — stable mount point for Phase 2 React island
- FAQ component imports `FAQ_ITEMS` from `src/data/faq.ts` (same array as JSON-LD schema)
- All 11 Playwright `@smoke` tests green (covers all BRAND-01–03, SEO-01–08)
- Human visual verification approved
- Phase 1 gate: site is deployable, crawlable, and fully SEO-instrumented
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
