---
phase: 09-saved-qr-library-pro-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/library/save-api.spec.ts
  - tests/library/list-api.spec.ts
  - tests/library/update-api.spec.ts
  - tests/library/delete-api.spec.ts
  - tests/gates/pro-gates.spec.ts
autonomous: true
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-04
  - GATE-01
  - GATE-02
  - GATE-03

must_haves:
  truths:
    - "Test stubs exist for all five spec files and are marked test.fixme (not skipped, not failing)"
    - "`npm run test:smoke` exits 0 with all new tests shown as 'fixme' in the report"
  artifacts:
    - path: "tests/library/save-api.spec.ts"
      provides: "Save API unauthenticated 401 + non-Pro 403 smoke stubs"
    - path: "tests/library/list-api.spec.ts"
      provides: "QR List API unauthenticated 401 smoke stub"
    - path: "tests/library/update-api.spec.ts"
      provides: "QR Update API unauthenticated 401 smoke stub"
    - path: "tests/library/delete-api.spec.ts"
      provides: "QR Delete API unauthenticated 401 smoke stub"
    - path: "tests/gates/pro-gates.spec.ts"
      provides: "Anonymous user gate smoke stubs (GATE-01, GATE-02, GATE-03)"
  key_links:
    - from: "tests/library/save-api.spec.ts"
      to: "/api/qr/save"
      via: "POST request to not-yet-created endpoint"
      pattern: "test.fixme"
    - from: "tests/gates/pro-gates.spec.ts"
      to: "src/components/QRGeneratorIsland.tsx"
      via: "Anonymous homepage UI checks"
      pattern: "test.fixme"
---

<objective>
Create Wave 0 test scaffolds for all Phase 9 features so subsequent plans have Nyquist-compliant verification from the start.

Purpose: Nyquist rule — every task must have an `<automated>` verify command. These stubs use `test.fixme` (project-established pattern from Phase 7) so they appear visibly in reports but do not block the smoke suite.
Output: 5 spec files, all stubs marked test.fixme, `npm run test:smoke` exits 0.
</objective>

<execution_context>
@/Users/ranjit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ranjit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-saved-qr-library-pro-gates/09-CONTEXT.md
@.planning/phases/09-saved-qr-library-pro-gates/09-RESEARCH.md
@.planning/phases/09-saved-qr-library-pro-gates/09-VALIDATION.md

<interfaces>
<!-- Existing test pattern from Phase 7 -->
<!-- Source: tests/ directory — established test.fixme pattern -->

Playwright config: playwright.config.ts
Quick smoke: npm run test:smoke  (runs --grep @smoke)
Full suite: npm run test

Phase 7 stub pattern:
```typescript
import { test, expect } from '@playwright/test';
test.fixme('description @smoke', async ({ page }) => {
  // TODO: implement after Wave N builds the feature
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API smoke stubs (LIB-01 through LIB-04)</name>
  <files>
    tests/library/save-api.spec.ts,
    tests/library/list-api.spec.ts,
    tests/library/update-api.spec.ts,
    tests/library/delete-api.spec.ts
  </files>
  <action>
Create the `tests/library/` directory and four spec files with `test.fixme` stubs (project pattern established in Phase 7 — use `test.fixme`, NOT `test.skip`).

**tests/library/save-api.spec.ts** — tag @smoke, cover:
- `Save API @smoke` stub 1: unauthenticated POST /api/qr/save returns 401
- `Save API @smoke` stub 2: authenticated non-Pro POST /api/qr/save returns 403

**tests/library/list-api.spec.ts** — tag @smoke, cover:
- `QR List API @smoke` stub: unauthenticated GET /api/qr/list returns 401

**tests/library/update-api.spec.ts** — tag @smoke, cover:
- `QR Update API @smoke` stub: unauthenticated PUT /api/qr/[id] returns 401

**tests/library/delete-api.spec.ts** — tag @smoke, cover:
- `QR Delete API @smoke` stub: unauthenticated DELETE /api/qr/[id] returns 401

All four files use `test.fixme` bodies with a comment: `// TODO: endpoint created in plan 09-02`. No actual fetch calls — stubs only. Import `{ test, expect }` from `@playwright/test`.
  </action>
  <verify>
    <automated>npm run test:smoke -- --grep "Save API|QR List API|QR Update API|QR Delete API" 2>&1 | grep -E "fixme|passed|failed" | head -20</automated>
  </verify>
  <done>All 5 stubs (2 in save-api.spec.ts, 1 each in the other three) appear as fixme in Playwright output. Zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create Pro gates smoke stubs (GATE-01, GATE-02, GATE-03)</name>
  <files>tests/gates/pro-gates.spec.ts</files>
  <action>
Create the `tests/gates/` directory and `pro-gates.spec.ts` with `test.fixme` stubs.

**tests/gates/pro-gates.spec.ts** — tag @smoke, cover:
- `Pro gates @smoke` stub 1 (GATE-01): Anonymous user — logo section renders drop-zone, not lock overlay
- `Pro gates @smoke` stub 2 (GATE-02): Anonymous user — classy dot shape button is clickable (no lock overlay)
- `Pro gates @smoke` stub 3 (GATE-03): Anonymous user — no "Save to Library" button visible on homepage

All three use `test.fixme` bodies with comment: `// TODO: QRGeneratorIsland auth-awareness added in plan 09-03`. No page navigation — stubs only. Import `{ test, expect }` from `@playwright/test`.
  </action>
  <verify>
    <automated>npm run test:smoke -- --grep "Pro gates" 2>&1 | grep -E "fixme|passed|failed" | head -10</automated>
  </verify>
  <done>3 stubs appear as fixme. Zero failures. `npm run test:smoke` overall exits 0.</done>
</task>

</tasks>

<verification>
After both tasks: `npm run test:smoke` exits 0. All new stubs show as fixme (not failed, not skipped). Directory structure `tests/library/` and `tests/gates/` exists with 5 spec files total.
</verification>

<success_criteria>
- 5 new spec files created: tests/library/save-api.spec.ts, list-api.spec.ts, update-api.spec.ts, delete-api.spec.ts, tests/gates/pro-gates.spec.ts
- All stubs use test.fixme (not test.skip)
- npm run test:smoke exits 0
- Zero test failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/09-saved-qr-library-pro-gates/09-01-SUMMARY.md`
</output>
