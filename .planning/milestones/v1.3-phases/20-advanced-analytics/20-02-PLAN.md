---
phase: 20-advanced-analytics
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/pages/dashboard/analytics/[slug].astro
  - src/components/analytics/DateRangePicker.tsx
  - src/components/analytics/AnalyticsDashboard.tsx
  - src/components/analytics/UtmBreakdown.tsx
autonomous: true
requirements:
  - ANALYTICS-01
  - ANALYTICS-02
  - ANALYTICS-04

must_haves:
  truths:
    - "User can select a custom date range and see all analytics update for that range"
    - "User can click Export CSV and download scan data with UTM columns"
    - "Analytics page shows UTM breakdown chart for source, medium, and campaign"
  artifacts:
    - path: "src/components/analytics/DateRangePicker.tsx"
      provides: "Date range selection UI with preset ranges"
      min_lines: 30
    - path: "src/components/analytics/AnalyticsDashboard.tsx"
      provides: "Client-side analytics container that fetches data with date range params"
      min_lines: 50
    - path: "src/components/analytics/UtmBreakdown.tsx"
      provides: "UTM source/medium/campaign breakdown display"
      min_lines: 20
    - path: "src/pages/dashboard/analytics/[slug].astro"
      provides: "Updated analytics page wiring AnalyticsDashboard island"
      contains: "AnalyticsDashboard"
  key_links:
    - from: "src/components/analytics/AnalyticsDashboard.tsx"
      to: "/api/analytics/[slug]"
      via: "fetch with from/to query params"
      pattern: "fetch.*api/analytics.*from=.*to="
    - from: "src/components/analytics/AnalyticsDashboard.tsx"
      to: "src/components/analytics/DateRangePicker.tsx"
      via: "onRangeChange callback"
      pattern: "DateRangePicker"
    - from: "src/components/analytics/AnalyticsDashboard.tsx"
      to: "src/components/analytics/UtmBreakdown.tsx"
      via: "passes utm data as props"
      pattern: "UtmBreakdown"
---

<objective>
Build the analytics UI: date range picker, CSV export button, and UTM breakdown chart. Refactor the analytics page from server-rendered to a client-side React island that re-fetches data when the date range changes.

Purpose: Users can now interrogate scan data across any date range, export it, and see UTM parameter breakdowns -- completing all four ANALYTICS requirements.
Output: Interactive analytics dashboard with date picker, CSV export, and UTM charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-advanced-analytics/20-01-SUMMARY.md

@src/pages/dashboard/analytics/[slug].astro
@src/components/analytics/ScanChart.tsx
@src/components/analytics/ProgressBarList.tsx

<interfaces>
<!-- From Plan 01: Analytics API response shape (after date range + UTM additions) -->
```typescript
// GET /api/analytics/[slug]?from=UNIX&to=UNIX
// Response:
{
  name: string;
  slug: string;
  total: number;
  unique: number;
  timeSeries: { date: string; scans: number }[];
  devices: { device: string; scans: number }[];
  countries: { country: string; scans: number }[];
  utm: {
    sources: { value: string; scans: number }[];
    mediums: { value: string; scans: number }[];
    campaigns: { value: string; scans: number }[];
  };
}
```

From src/components/analytics/ScanChart.tsx:
```typescript
interface ScanChartProps {
  data: { date: string; scans: number }[];
}
export default function ScanChart({ data }: ScanChartProps)
```

From src/components/analytics/ProgressBarList.tsx:
```typescript
// Accepts: heading: string, items: { label: string; scans: number; pct: number }[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DateRangePicker, UtmBreakdown, and AnalyticsDashboard components</name>
  <files>src/components/analytics/DateRangePicker.tsx, src/components/analytics/UtmBreakdown.tsx, src/components/analytics/AnalyticsDashboard.tsx</files>
  <read_first>src/components/analytics/ScanChart.tsx, src/components/analytics/ProgressBarList.tsx, src/pages/dashboard/analytics/[slug].astro</read_first>
  <action>
1. **DateRangePicker.tsx** -- A React component with:
   - Props: `onRangeChange: (from: number, to: number) => void`
   - Preset buttons row: "Last 7 days", "Last 30 days", "Last 90 days", "Last 12 months", "All time"
   - Two native `<input type="date">` fields for custom start/end dates (no third-party date picker library)
   - "Last 30 days" is selected by default (highlighted with indigo bg)
   - When a preset is clicked, compute `from` (Unix seconds) and `to` (now Unix seconds) and call `onRangeChange`
   - When custom dates are changed, convert to Unix seconds and call `onRangeChange`
   - "All time" passes `from=0, to=now`
   - Tailwind styling consistent with existing analytics components (white/slate-800 bg, rounded-xl, border)

2. **UtmBreakdown.tsx** -- A React component with:
   - Props: `utm: { sources: { value: string; scans: number }[]; mediums: { value: string; scans: number }[]; campaigns: { value: string; scans: number }[] }`
   - Renders three columns (grid, 1 col on mobile, 3 on lg) with ProgressBarList-style bars for each UTM dimension
   - Each column has a heading: "UTM Source", "UTM Medium", "UTM Campaign"
   - If all three arrays are empty, show a muted message: "No UTM data yet. Add UTM parameters to your destination URLs to track campaign performance."
   - Compute pct values relative to the max within each dimension (largest bar = 100%)
   - Tailwind styling matching existing analytics cards

3. **AnalyticsDashboard.tsx** -- The main React island that replaces server-side data fetching:
   - Props: `slug: string, initialFrom: number, initialTo: number` (initialFrom/To default to last 30 days)
   - State: `data` (analytics response | null), `loading` (boolean), `from` (number), `to` (number)
   - On mount and whenever from/to change, fetch `/api/analytics/${slug}?from=${from}&to=${to}`
   - Show a loading skeleton while fetching (simple pulse animation placeholder)
   - Render in order:
     a. DateRangePicker (passes onRangeChange that sets from/to state)
     b. Stat cards row (Total Scans, ~Unique Scans) -- same Tailwind as existing
     c. Export CSV button (right-aligned, above the chart) -- see CSV export logic below
     d. ScanChart with data.timeSeries
     e. Device Breakdown (ProgressBarList)
     f. Top Countries (ProgressBarList)
     g. UtmBreakdown with data.utm
   - **CSV export logic:** When "Export CSV" is clicked:
     - Fetch raw scan events: `GET /api/analytics/${slug}/export?from=${from}&to=${to}` -- BUT to avoid a new endpoint, build CSV from the existing aggregated data. Actually, the requirement says "one row per scan event" so we need a dedicated export endpoint. Instead, implement CSV generation client-side:
     - Actually, re-read the requirement: "download a file with one row per scan event including date, device, country, and UTM parameters". This needs raw events. Create a small fetch to a new export endpoint (Task 2 will add it). For now, wire the button to call `/api/analytics/${slug}/export?from=${from}&to=${to}` and trigger a download of the response as a .csv file.
     - Button: "Export CSV" with a download icon, styled as secondary button (border, not filled)
   - Import existing ScanChart and ProgressBarList components
   - COUNTRY_NAMES map: copy from the existing [slug].astro file into this component (or extract to a shared util)
   - Device label mapping: 'ios' -> 'iOS', 'android' -> 'Android', 'desktop' -> 'Desktop', else 'Other'
   - toPct helper: same as existing `(n / total) * 100` rounded
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DateRangePicker" src/components/analytics/DateRangePicker.tsx
    - grep -q "onRangeChange" src/components/analytics/DateRangePicker.tsx
    - grep -q "UtmBreakdown" src/components/analytics/UtmBreakdown.tsx
    - grep -q "utm" src/components/analytics/UtmBreakdown.tsx
    - grep -q "AnalyticsDashboard" src/components/analytics/AnalyticsDashboard.tsx
    - grep -q "Export CSV" src/components/analytics/AnalyticsDashboard.tsx
    - grep -q "DateRangePicker" src/components/analytics/AnalyticsDashboard.tsx
    - grep -q "UtmBreakdown" src/components/analytics/AnalyticsDashboard.tsx
    - grep -q "ScanChart" src/components/analytics/AnalyticsDashboard.tsx
  </acceptance_criteria>
  <done>Three new React components created: DateRangePicker with presets and custom date inputs, UtmBreakdown displaying three UTM dimensions, AnalyticsDashboard orchestrating all analytics UI with client-side data fetching and CSV export trigger</done>
</task>

<task type="auto">
  <name>Task 2: Add CSV export endpoint and wire AnalyticsDashboard into analytics page</name>
  <files>src/pages/api/analytics/[slug]/export.ts, src/pages/dashboard/analytics/[slug].astro</files>
  <read_first>src/pages/dashboard/analytics/[slug].astro, src/pages/api/analytics/[slug].ts, src/components/analytics/AnalyticsDashboard.tsx</read_first>
  <action>
1. **Create src/pages/api/analytics/[slug]/export.ts** -- CSV export API endpoint:
   - `export const prerender = false;`
   - GET handler with same auth + Pro gate + ownership check as the existing analytics API (copy the pattern from src/pages/api/analytics/[slug].ts)
   - Accept `from` and `to` query params (Unix seconds), default to 30 days ago / now
   - Query: `SELECT scannedAt, device, country, utmSource, utmMedium, utmCampaign FROM scan_events WHERE dynamicQrCodeId = ? AND scannedAt >= ? AND scannedAt <= ? ORDER BY scannedAt DESC LIMIT 10000`
   - Use Drizzle select with the appropriate columns and filters (eq, gte, lte, desc)
   - Build CSV string with native JS (no library needed):
     - Header row: `Date,Device,Country,UTM Source,UTM Medium,UTM Campaign`
     - For each row: convert scannedAt (Unix seconds) to ISO date string `new Date(row.scannedAt * 1000).toISOString()`, escape any commas in field values by wrapping in double quotes
   - Return with `Content-Type: text/csv`, `Content-Disposition: attachment; filename="scans-{slug}-{from}-{to}.csv"`
   - Limit to 10,000 rows to prevent memory issues

2. **Update src/pages/dashboard/analytics/[slug].astro**:
   - Remove ALL the server-side analytics data fetching (the Promise.all with 5 queries, the timeSeries computation, deviceItems, countryItems, analyticsData construction, toPct helper, COUNTRY_NAMES) -- this all moves client-side into AnalyticsDashboard
   - Keep: the frontmatter auth check, Pro gate, ownership check (dynamicQr query), and the page layout structure
   - Replace the analytics rendering section with a single React island:
     ```astro
     <AnalyticsDashboard
       slug={dynamicQr.qrSlug}
       name={dynamicQr.name}
       client:only="react"
     />
     ```
   - Import AnalyticsDashboard from the components directory
   - Keep the back link, page header (name + slug), and error state
   - Keep the Toaster component
   - Remove ScanChart and ProgressBarList imports (now imported by AnalyticsDashboard internally)
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx astro check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/pages/api/analytics/[slug]/export.ts
    - grep -q "prerender = false" "src/pages/api/analytics/[slug]/export.ts"
    - grep -q "text/csv" "src/pages/api/analytics/[slug]/export.ts"
    - grep -q "Content-Disposition" "src/pages/api/analytics/[slug]/export.ts"
    - grep -q "AnalyticsDashboard" "src/pages/dashboard/analytics/[slug].astro"
    - grep -q 'client:only="react"' "src/pages/dashboard/analytics/[slug].astro"
  </acceptance_criteria>
  <done>CSV export endpoint returns scan events as downloadable CSV with date, device, country, and UTM columns; analytics page uses AnalyticsDashboard React island instead of server-rendered data; all four ANALYTICS requirements are wired end-to-end</done>
</task>

</tasks>

<verification>
- `npx astro check` passes with no type errors
- `npx astro build` succeeds
- Analytics page loads at /dashboard/analytics/[slug] and renders the AnalyticsDashboard island
- DateRangePicker shows preset buttons and custom date inputs
- Changing date range re-fetches data and updates all charts
- Export CSV button triggers a file download
- UTM breakdown section renders (empty state if no UTM data yet)
</verification>

<success_criteria>
- User can select preset date ranges (7d, 30d, 90d, 12mo, all time) or custom dates
- All analytics panels (stats, chart, devices, countries) update when date range changes
- Export CSV downloads a file with one row per scan event including UTM columns
- UTM breakdown shows source, medium, and campaign charts when UTM data exists
- Page is backward compatible -- works the same as before when no date range is selected (defaults to 30 days)
</success_criteria>

<output>
After completion, create `.planning/phases/20-advanced-analytics/20-02-SUMMARY.md`
</output>
