---
phase: 22-seasonal-template-packs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/frames.ts
  - src/data/templateData.ts
  - src/components/SeasonalTemplates.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - TEMPLATE-01
  - TEMPLATE-02

must_haves:
  truths:
    - "Template picker shows 20+ seasonal/holiday presets across Christmas, Halloween, Valentine's Day, Easter, Black Friday, Summer, and Back to School categories"
    - "Applying a seasonal template updates QR preview with correct frame, colors, and dot style without page reload"
    - "Homepage displays a seasonal section showing templates relevant to the current calendar period"
    - "Seasonal section content changes based on the month (e.g., December shows Christmas templates)"
  artifacts:
    - path: "src/data/templateData.ts"
      provides: "Seasonal template preset objects in TEMPLATES array"
      contains: "seasonal-christmas"
    - path: "src/types/frames.ts"
      provides: "Updated TemplatePreset category union with seasonal categories"
      contains: "Christmas"
    - path: "src/components/SeasonalTemplates.astro"
      provides: "Homepage seasonal collection section"
      min_lines: 30
    - path: "src/pages/index.astro"
      provides: "SeasonalTemplates import and placement"
      contains: "SeasonalTemplates"
  key_links:
    - from: "src/data/templateData.ts"
      to: "src/components/customize/TemplateSection.tsx"
      via: "TEMPLATES and TEMPLATE_CATEGORIES imports"
      pattern: "TEMPLATES.*filter"
    - from: "src/data/templateData.ts"
      to: "src/components/SeasonalTemplates.astro"
      via: "TEMPLATES import for seasonal filtering"
      pattern: "import.*TEMPLATES"
    - from: "src/components/SeasonalTemplates.astro"
      to: "src/pages/index.astro"
      via: "component import"
      pattern: "SeasonalTemplates"
---

<objective>
Add 20+ seasonal/holiday template presets to the existing template system and create a homepage seasonal collection section that surfaces calendar-relevant templates.

Purpose: Drive re-engagement by offering themed QR designs for holidays and seasons, and surface them on the homepage when they are timely.
Output: Expanded template data, updated types, new homepage section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/types/frames.ts
@src/data/templateData.ts
@src/components/customize/TemplateSection.tsx
@src/pages/index.astro
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/frames.ts:
```typescript
export type FrameType =
  | "none"
  | "simple-border"
  | "rounded-border"
  | "top-bottom"
  | "bottom-banner"
  | "badge"
  | "shopping-bag"
  | "clipboard";

export interface TemplatePreset {
  id: string;
  name: string;
  category: "Minimal" | "Bold" | "Business" | "Vibrant";
  frameType: FrameType;
  dotColor: string;
  bgColor: string;
  dotType: DotType;
  cornerSquareType: CornerSquareType;
  frameText: string;
}
```

From src/data/templateData.ts:
```typescript
export const TEMPLATE_CATEGORIES = ["Minimal", "Bold", "Business", "Vibrant"] as const;
export const TEMPLATES: TemplatePreset[] = [ /* 16 presets */ ];
```

From src/components/customize/TemplateSection.tsx:
```typescript
// Renders categories from TEMPLATE_CATEGORIES, filters TEMPLATES by category
// onApply callback applies preset to QRGeneratorIsland state
export function TemplateSection({ selectedId, onApply }: TemplateSectionProps)
```

Homepage section pattern (from UseCasesTeaser.astro):
```astro
<section class="bg-gray-50 dark:bg-[#0f172a] py-16 sm:py-20">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- heading, grid of cards, CTA link -->
  </div>
</section>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add 22 seasonal template presets to the template data system</name>
  <files>src/types/frames.ts, src/data/templateData.ts</files>
  <read_first>
    - src/types/frames.ts (TemplatePreset interface, category union type)
    - src/data/templateData.ts (existing TEMPLATES array structure, TEMPLATE_CATEGORIES)
    - src/components/customize/TemplateSection.tsx (how categories are rendered — must keep working)
  </read_first>
  <acceptance_criteria>
    - TEMPLATE_CATEGORIES contains 7+ seasonal categories in addition to the original 4
    - TEMPLATES array contains 22+ new seasonal presets (38+ total)
    - TemplatePreset.category union type includes all new category names
    - TemplateSection still renders correctly (no TypeScript errors)
    - Each preset has a unique id, meaningful name, appropriate colors/styles for its holiday
  </acceptance_criteria>
  <action>
    **Per TEMPLATE-01:** Add 22 seasonal/holiday template presets covering 7 seasonal categories.

    1. In `src/types/frames.ts`, update the `TemplatePreset` interface `category` field from:
       ```
       category: "Minimal" | "Bold" | "Business" | "Vibrant";
       ```
       to:
       ```
       category: "Minimal" | "Bold" | "Business" | "Vibrant" | "Christmas" | "Halloween" | "Valentine's Day" | "Easter" | "Black Friday" | "Summer" | "Back to School";
       ```

    2. In `src/data/templateData.ts`, add the new categories to TEMPLATE_CATEGORIES:
       ```typescript
       export const TEMPLATE_CATEGORIES = [
         "Minimal", "Bold", "Business", "Vibrant",
         "Christmas", "Halloween", "Valentine's Day", "Easter",
         "Black Friday", "Summer", "Back to School",
       ] as const;
       ```

    3. Add 22 seasonal presets to the TEMPLATES array after the existing 16. Use these specifications:

       **Christmas (4 presets):**
       - `seasonal-christmas-red`: "Festive Red" — dotColor: #b91c1c, bgColor: #fef2f2, dotType: dots, cornerSquareType: extra-rounded, frameType: badge, frameText: "Merry Christmas"
       - `seasonal-christmas-green`: "Holiday Green" — dotColor: #166534, bgColor: #f0fdf4, dotType: rounded, cornerSquareType: extra-rounded, frameType: bottom-banner, frameText: "Happy Holidays"
       - `seasonal-christmas-gold`: "Christmas Gold" — dotColor: #92400e, bgColor: #fffbeb, dotType: extra-rounded, cornerSquareType: extra-rounded, frameType: simple-border, frameText: "Season's Greetings"
       - `seasonal-christmas-classic`: "Classic Christmas" — dotColor: #991b1b, bgColor: #ffffff, dotType: square, cornerSquareType: square, frameType: top-bottom, frameText: "Scan for Gift"

       **Halloween (3 presets):**
       - `seasonal-halloween-orange`: "Pumpkin Patch" — dotColor: #c2410c, bgColor: #fff7ed, dotType: dots, cornerSquareType: dot, frameType: badge, frameText: "Trick or Treat"
       - `seasonal-halloween-dark`: "Spooky Night" — dotColor: #581c87, bgColor: #faf5ff, dotType: extra-rounded, cornerSquareType: extra-rounded, frameType: rounded-border, frameText: "Happy Halloween"
       - `seasonal-halloween-black`: "Midnight Black" — dotColor: #1c1917, bgColor: #fafaf9, dotType: square, cornerSquareType: square, frameType: bottom-banner, frameText: "Scan if You Dare"

       **Valentine's Day (3 presets):**
       - `seasonal-valentine-pink`: "Sweet Pink" — dotColor: #be185d, bgColor: #fdf2f8, dotType: dots, cornerSquareType: extra-rounded, frameType: badge, frameText: "Be Mine"
       - `seasonal-valentine-red`: "Love Letter" — dotColor: #dc2626, bgColor: #fff1f2, dotType: rounded, cornerSquareType: extra-rounded, frameType: simple-border, frameText: "With Love"
       - `seasonal-valentine-rose`: "Rose Garden" — dotColor: #9f1239, bgColor: #fff1f2, dotType: extra-rounded, cornerSquareType: dot, frameType: bottom-banner, frameText: "Happy Valentine's"

       **Easter (3 presets):**
       - `seasonal-easter-pastel`: "Pastel Spring" — dotColor: #7c3aed, bgColor: #faf5ff, dotType: dots, cornerSquareType: extra-rounded, frameType: rounded-border, frameText: "Happy Easter"
       - `seasonal-easter-green`: "Spring Garden" — dotColor: #15803d, bgColor: #f0fdf4, dotType: rounded, cornerSquareType: extra-rounded, frameType: badge, frameText: "Easter Greetings"
       - `seasonal-easter-yellow`: "Sunny Easter" — dotColor: #a16207, bgColor: #fefce8, dotType: extra-rounded, cornerSquareType: extra-rounded, frameType: simple-border, frameText: "Spring Wishes"

       **Black Friday (3 presets):**
       - `seasonal-bf-dark`: "Midnight Deal" — dotColor: #18181b, bgColor: #fafafa, dotType: square, cornerSquareType: square, frameType: top-bottom, frameText: "Black Friday Deal"
       - `seasonal-bf-gold`: "Gold Rush" — dotColor: #92400e, bgColor: #18181b, dotType: dots, cornerSquareType: extra-rounded, frameType: badge, frameText: "Shop Now"
       - `seasonal-bf-red`: "Hot Sale" — dotColor: #dc2626, bgColor: #18181b, dotType: extra-rounded, cornerSquareType: dot, frameType: bottom-banner, frameText: "Limited Offer"

       **Summer (3 presets):**
       - `seasonal-summer-ocean`: "Ocean Breeze" — dotColor: #0369a1, bgColor: #f0f9ff, dotType: dots, cornerSquareType: extra-rounded, frameType: rounded-border, frameText: "Summer Vibes"
       - `seasonal-summer-sunset`: "Sunset Glow" — dotColor: #ea580c, bgColor: #fff7ed, dotType: rounded, cornerSquareType: extra-rounded, frameType: badge, frameText: "Summer Fun"
       - `seasonal-summer-tropical`: "Tropical" — dotColor: #0d9488, bgColor: #f0fdfa, dotType: extra-rounded, cornerSquareType: extra-rounded, frameType: simple-border, frameText: "Aloha"

       **Back to School (3 presets):**
       - `seasonal-school-blue`: "Scholar Blue" — dotColor: #1d4ed8, bgColor: #eff6ff, dotType: rounded, cornerSquareType: extra-rounded, frameType: clipboard, frameText: "Back to School"
       - `seasonal-school-green`: "Chalkboard" — dotColor: #166534, bgColor: #f0fdf4, dotType: square, cornerSquareType: square, frameType: bottom-banner, frameText: "Class Info"
       - `seasonal-school-pencil`: "Pencil Yellow" — dotColor: #a16207, bgColor: #fefce8, dotType: extra-rounded, cornerSquareType: extra-rounded, frameType: simple-border, frameText: "Scan to Learn"

    4. Verify TypeScript compiles with `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - TEMPLATES array has 38 total presets (16 original + 22 seasonal)
    - TEMPLATE_CATEGORIES has 11 entries (4 original + 7 seasonal)
    - TemplatePreset category type includes all 11 categories
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create homepage seasonal collection section with calendar-based filtering</name>
  <files>src/components/SeasonalTemplates.astro, src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current section order, import pattern)
    - src/components/UseCasesTeaser.astro (homepage section styling pattern — dark mode, spacing, grid)
    - src/data/templateData.ts (after Task 1 — seasonal template data)
  </read_first>
  <acceptance_criteria>
    - SeasonalTemplates.astro component exists and renders a grid of seasonal templates
    - Templates shown are filtered by current month/date to the relevant season
    - Each template card shows a color swatch preview (dot color + bg color), the template name, and a CTA link to the generator
    - Component follows the same styling pattern as other homepage sections (dark mode, max-w-6xl, etc.)
    - index.astro imports and renders SeasonalTemplates between PricingPromo and UseCasesTeaser
    - Build succeeds
  </acceptance_criteria>
  <action>
    **Per TEMPLATE-02:** Create a homepage seasonal section that shows calendar-relevant templates.

    1. Create `src/components/SeasonalTemplates.astro`:

       **Calendar mapping logic** (in the frontmatter script):
       ```
       Month 1 (Jan)      → "Christmas" (post-holiday) + "Valentine's Day" (upcoming)
       Month 2 (Feb)      → "Valentine's Day"
       Month 3-4 (Mar-Apr)→ "Easter" + "Back to School" (spring semester)
       Month 5 (May)      → "Summer" (upcoming)
       Month 6-7 (Jun-Jul)→ "Summer"
       Month 8 (Aug)      → "Summer" + "Back to School"
       Month 9 (Sep)      → "Back to School"
       Month 10 (Oct)     → "Halloween"
       Month 11 (Nov)     → "Black Friday" + "Christmas" (upcoming)
       Month 12 (Dec)     → "Christmas"
       ```

       The component should:
       - Import TEMPLATES from `../../data/templateData.ts` (Astro uses `../` since components/ is one level from src/)
       - Get the current month via `new Date().getMonth() + 1`
       - Look up the active seasonal categories for this month from the mapping
       - Filter TEMPLATES to only those whose category is in the active set
       - Derive a section title from the primary active season (e.g., "Christmas Templates" in December, "Summer Templates" in June)

       **Template card rendering:**
       Each card in a responsive grid (grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4):
       - A color swatch preview: 64x64 div with the template's bgColor as background, containing a centered 40x40 inner div with the template's dotColor (rounded-lg, to suggest a mini QR)
       - Template name below the swatch
       - The entire card is an `<a>` link to `/#qr-generator` (scrolls to the generator section)
       - Hover: border-blue-500 transition

       **Section styling** (match UseCasesTeaser pattern):
       ```html
       <section class="bg-white dark:bg-slate-900 py-16 sm:py-20">
         <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
           <h2><!-- dynamic title --></h2>
           <p><!-- subtitle --></p>
           <ul role="list" class="grid ...">
             <!-- template cards -->
           </ul>
           <div class="text-center mt-8">
             <a href="/#qr-generator">Browse all templates →</a>
           </div>
         </div>
       </section>
       ```

    2. In `src/pages/index.astro`:
       - Add import: `import SeasonalTemplates from '../components/SeasonalTemplates.astro';`
       - Place `<SeasonalTemplates />` between `<PricingPromo />` and `<UseCasesTeaser />` in the main element

    3. Run `npm run build` to verify the full build succeeds.
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - SeasonalTemplates.astro renders a grid of templates relevant to the current month
    - Homepage shows the seasonal section between pricing and use cases
    - Section title reflects the current season (e.g., "Easter Templates" in March)
    - Build completes without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles with updated types and data
2. `npm run build` — Full Astro build succeeds
3. Visual: Template picker in the generator shows seasonal categories alongside original ones
4. Visual: Homepage has a seasonal section with calendar-appropriate templates
</verification>

<success_criteria>
- 22+ seasonal template presets exist across 7 holiday/season categories
- Template picker renders all categories (original 4 + seasonal 7)
- Homepage seasonal section displays templates matching the current calendar period
- Applying any seasonal template updates the QR preview (same mechanism as existing templates — no new wiring needed)
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-seasonal-template-packs/22-01-SUMMARY.md`
</output>
