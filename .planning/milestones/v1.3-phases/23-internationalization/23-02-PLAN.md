---
phase: 23-internationalization
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/pages/es/index.astro
  - src/pages/es/pricing.astro
  - src/pages/es/use-cases/index.astro
  - src/pages/fr/index.astro
  - src/pages/fr/pricing.astro
  - src/pages/fr/use-cases/index.astro
  - src/pages/de/index.astro
  - src/pages/de/pricing.astro
  - src/pages/de/use-cases/index.astro
  - tests/i18n.spec.ts
autonomous: false
requirements: [I18N-01, I18N-02, I18N-03]

must_haves:
  truths:
    - "Homepage is fully readable in Spanish at /es/"
    - "Homepage is fully readable in French at /fr/"
    - "Homepage is fully readable in German at /de/"
    - "Pricing page is fully readable in Spanish at /es/pricing"
    - "Pricing page is fully readable in French at /fr/pricing"
    - "Pricing page is fully readable in German at /de/pricing"
    - "Use cases index is fully readable in Spanish at /es/use-cases/"
    - "Use cases index is fully readable in French at /fr/use-cases/"
    - "Use cases index is fully readable in German at /de/use-cases/"
    - "Language switcher navigates between locale variants"
    - "All translated pages have hreflang tags in head"
  artifacts:
    - path: "src/pages/es/index.astro"
      provides: "Spanish homepage"
      contains: "useTranslations('es')"
    - path: "src/pages/fr/index.astro"
      provides: "French homepage"
      contains: "useTranslations('fr')"
    - path: "src/pages/de/index.astro"
      provides: "German homepage"
      contains: "useTranslations('de')"
    - path: "src/pages/es/pricing.astro"
      provides: "Spanish pricing page"
      contains: "useTranslations('es')"
    - path: "src/pages/fr/pricing.astro"
      provides: "French pricing page"
      contains: "useTranslations('fr')"
    - path: "src/pages/de/pricing.astro"
      provides: "German pricing page"
      contains: "useTranslations('de')"
    - path: "tests/i18n.spec.ts"
      provides: "Playwright smoke tests for i18n"
      contains: "i18n"
  key_links:
    - from: "src/pages/es/index.astro"
      to: "src/i18n/utils.ts"
      via: "useTranslations import"
      pattern: "useTranslations\\('es'\\)"
    - from: "src/pages/es/index.astro"
      to: "src/layouts/Layout.astro"
      via: "Layout component with lang prop"
      pattern: "lang=\"es\""
    - from: "src/pages/es/index.astro"
      to: "src/components/Header.astro"
      via: "Header component with lang prop"
      pattern: "lang=\"es\""
---

<objective>
Create all 9 translated marketing pages (3 pages x 3 languages) and Playwright smoke tests verifying i18n functionality, hreflang tags, and language switcher behavior.

Purpose: This delivers the user-facing i18n content (I18N-01) and validates the infrastructure from Plan 01 works end-to-end. The Playwright tests confirm hreflang SEO compliance (I18N-03) and language switcher navigation (I18N-02).

Output: 9 translated .astro pages under es/, fr/, de/ directories + i18n.spec.ts test file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-internationalization/23-RESEARCH.md
@.planning/phases/23-internationalization/23-01-SUMMARY.md

@src/pages/index.astro
@src/pages/pricing.astro
@src/pages/use-cases/index.astro
@src/i18n/ui.ts
@src/i18n/utils.ts
@src/layouts/Layout.astro
@src/components/Header.astro

<interfaces>
<!-- From Plan 01 — executor should use these directly -->

From src/i18n/utils.ts:
```typescript
export function getLangFromUrl(url: URL): keyof typeof ui;
export function useTranslations(lang: keyof typeof ui): (key: UiKeys) => string;
export function getLocalizedHref(path: string, lang: keyof typeof ui): string;
```

From src/i18n/ui.ts:
```typescript
export const languages: { en: 'English', es: 'Espanol', fr: 'Francais', de: 'Deutsch' };
export const defaultLang = 'en';
export const ui: Record<'en' | 'es' | 'fr' | 'de', Record<string, string>>;
export type UiKeys = keyof typeof ui[typeof defaultLang];
```

From src/layouts/Layout.astro:
```typescript
interface Props {
  title: string;
  description: string;
  ogImage?: string;
  lang?: string;  // Added in Plan 01
}
```

From src/components/Header.astro:
```typescript
interface Props {
  lang?: string;  // Added in Plan 01, default 'en'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 9 translated marketing pages</name>
  <files>src/pages/es/index.astro, src/pages/es/pricing.astro, src/pages/es/use-cases/index.astro, src/pages/fr/index.astro, src/pages/fr/pricing.astro, src/pages/fr/use-cases/index.astro, src/pages/de/index.astro, src/pages/de/pricing.astro, src/pages/de/use-cases/index.astro</files>
  <read_first>
    - src/pages/index.astro (full file — use as template for es/fr/de homepage)
    - src/pages/pricing.astro (full file — use as template for es/fr/de pricing)
    - src/pages/use-cases/index.astro (full file — use as template for es/fr/de use-cases)
    - src/i18n/ui.ts (translation keys available — use t() for all visible strings)
    - src/i18n/utils.ts (useTranslations, getLocalizedHref API)
    - src/layouts/Layout.astro (verify lang prop exists from Plan 01)
    - src/components/Header.astro (verify lang prop exists from Plan 01)
    - .planning/phases/23-internationalization/23-RESEARCH.md (Pattern 4 for exact page structure)
  </read_first>
  <action>
    For each locale (es, fr, de), create 3 pages by duplicating the English page structure and replacing hardcoded strings with `t()` calls.

    **Pattern for every translated page** (per Research Pattern 4):
    ```astro
    ---
    import { useTranslations } from '../../i18n/utils';  // adjust depth for page location
    import Layout from '../../layouts/Layout.astro';
    import Header from '../../components/Header.astro';
    // ... same imports as English page

    const t = useTranslations('es');  // or 'fr', 'de'
    ---
    <Layout title={t('meta.homeTitle')} description={t('meta.homeDescription')} lang="es">
      <Header lang="es" />
      <!-- Replace ALL hardcoded English strings with t('key') calls -->
    </Layout>
    ```

    **Homepage (es/index.astro, fr/index.astro, de/index.astro):**
    - Copy structure from src/pages/index.astro
    - Replace all component content strings with t() calls
    - Pass `lang` prop to Layout, Header, and all child components that accept it
    - For components that DON'T accept a lang prop (Hero, Features, HowTo, etc.): either (a) add lang prop to those components and use useTranslations inside, or (b) inline the translated content directly in the page. Choose (a) if the component is simple, (b) if the component has complex logic.
    - The HowTo JSON-LD schema should use translated step names/descriptions
    - Keep the same section structure and Tailwind classes as the English page

    **Pricing (es/pricing.astro, fr/pricing.astro, de/pricing.astro):**
    - Copy structure from src/pages/pricing.astro
    - Replace heading, subheading, tier names, feature descriptions, CTA buttons with t() calls
    - Keep pricing numbers ($0, $3.99, $9.99) as-is (prices don't change by locale)
    - Keep billing toggle logic identical

    **Use Cases Index (es/use-cases/index.astro, fr/use-cases/index.astro, de/use-cases/index.astro):**
    - Copy structure from src/pages/use-cases/index.astro
    - Replace page heading and subheading with t() calls
    - For use case card titles and descriptions, use t() calls with keys like `usecases.restaurant.title`, `usecases.restaurant.desc`
    - Individual use-case slug pages ([slug].astro) are English-only — do NOT create translated slug pages (Research Pitfall 6)
    - Links to individual use-case pages from the translated index should go to the English slug page

    **CRITICAL rules for all 9 pages:**
    - Every page passes `lang="es"` (or fr/de) to BOTH Layout and Header
    - Do NOT import from `astro:i18n` in these pages — use the `getLocalizedHref` helper from `src/i18n/utils.ts` for internal links
    - Adjust import paths based on directory depth (es/ pages use `../../`, es/use-cases/ pages use `../../../`)
    - Do NOT create translated versions of auth pages (login, signup), dashboard pages, or API routes
    - Keep all Tailwind classes identical to English versions
    - Breadcrumb JSON-LD schemas should use translated names where applicable
  </action>
  <acceptance_criteria>
    - 9 new .astro files created: 3 per locale under src/pages/es/, src/pages/fr/, src/pages/de/
    - Every file uses useTranslations with correct locale
    - Every file passes lang prop to Layout and Header
    - No hardcoded English strings in translated pages (all use t() calls)
    - Internal links use getLocalizedHref
    - No translated [slug].astro pages exist
    - Build succeeds: npx astro build produces output for /es/, /fr/, /de/ paths
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx astro build 2>&1 | tail -30 && ls dist/es/index.html dist/fr/index.html dist/de/index.html dist/es/pricing/index.html dist/fr/pricing/index.html dist/de/pricing/index.html dist/es/use-cases/index.html dist/fr/use-cases/index.html dist/de/use-cases/index.html 2>&1</automated>
  </verify>
  <done>All 9 translated pages build successfully. Each page uses t() for all visible strings. /es/, /fr/, /de/ URL prefixes are active. English URLs remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright i18n smoke tests</name>
  <files>tests/i18n.spec.ts</files>
  <read_first>
    - tests/foundation.spec.ts (existing test patterns — match test structure and conventions)
    - playwright.config.ts (test config — baseURL, test command)
    - src/i18n/ui.ts (get exact translation values for assertions)
  </read_first>
  <action>
    Create `tests/i18n.spec.ts` with Playwright smoke tests covering all three requirements.

    **I18N-01 tests (translated pages accessible):**
    - Test: `/es/` returns 200 and h1 contains the Spanish hero title (from ui.ts `es['hero.title']`)
    - Test: `/fr/` returns 200 and h1 contains the French hero title
    - Test: `/de/` returns 200 and h1 contains the German hero title
    - Test: `/es/pricing` returns 200 and contains Spanish pricing heading
    - Test: `/fr/pricing` returns 200 and contains French pricing heading
    - Test: `/de/pricing` returns 200 and contains German pricing heading
    - Test: `/es/use-cases/` returns 200
    - Test: `/fr/use-cases/` returns 200
    - Test: `/de/use-cases/` returns 200
    - Test: `/` still returns 200 with English content (regression — English URLs unchanged)

    **I18N-02 tests (language switcher):**
    - Test: Language switcher is visible on homepage (selector: `nav[aria-label="Language selection"]`)
    - Test: Clicking ES link navigates to /es/ and page shows Spanish content
    - Test: Language switcher on /es/ page shows current language as active (aria-current="true" on ES link)

    **I18N-03 tests (hreflang SEO):**
    - Test: Homepage `<head>` contains `<link rel="alternate" hreflang="es">`
    - Test: Homepage `<head>` contains `<link rel="alternate" hreflang="x-default">`
    - Test: `/es/` page `<head>` contains hreflang tags for all 4 locales + x-default
    - Test: hreflang URLs are absolute (start with https://qr-code-generator-app.com)

    Tag all tests with `@smoke` annotation or put them in a describe block that can be filtered. Match the test style and conventions of existing test files (use test.describe, page.goto, expect patterns from foundation.spec.ts).
  </action>
  <acceptance_criteria>
    - tests/i18n.spec.ts exists with tests covering I18N-01, I18N-02, I18N-03
    - Tests verify HTTP 200 for all 9 translated URLs + English regression
    - Tests verify language switcher visibility and navigation
    - Tests verify hreflang tags including x-default
    - Tests pass when run against dev server
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx playwright test tests/i18n.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <done>All i18n Playwright tests pass. Tests cover translated page accessibility, language switcher behavior, and hreflang SEO compliance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of translated pages and language switcher</name>
  <files>none</files>
  <action>
    Human verifies that all translated pages render correctly and the language switcher works as expected.
    What was built: 9 translated marketing pages (homepage, pricing, use-cases in Spanish, French, German), language switcher in header, hreflang SEO tags, sitemap i18n config.
    How to verify:
    1. Visit http://localhost:4321/ — English homepage loads normally, language switcher visible in header
    2. Click "ES" in language switcher — navigates to /es/, page shows Spanish content, header shows Spanish nav text
    3. Click "FR" in language switcher — navigates to /fr/, page shows French content
    4. Click "DE" in language switcher — navigates to /de/, page shows German content
    5. Visit /es/pricing — Spanish pricing page loads with correct tier names and features
    6. Visit /fr/use-cases/ — French use cases index loads
    7. View page source on /es/ — verify link rel="alternate" hreflang="x-default" is in head
    8. View page source on /es/ — verify html lang="es"
    9. Verify English URLs are unchanged: / still shows English, /pricing still shows English
    10. Spot-check translation quality — are the Spanish/French/German translations natural-sounding?
  </action>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx playwright test tests/i18n.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <done>User approves visual quality of all translated pages, language switcher navigation, and hreflang presence. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- `npx astro build` succeeds with all locale pages generated
- `npx playwright test tests/i18n.spec.ts` — all tests pass
- All 9 translated URLs return 200 with locale-specific content
- hreflang tags present on all marketing pages
- x-default hreflang points to English URL
- Language switcher navigates between locales
- English URLs unchanged (no /en/ prefix)
- Sitemap includes xhtml:link alternate entries
</verification>

<success_criteria>
- Homepage, pricing, and use-cases are fully readable in ES, FR, and DE (I18N-01)
- Language switcher works and navigates between locale variants (I18N-02)
- Hreflang tags present on all translated pages including x-default (I18N-03)
- Sitemap includes all language variants (I18N-03)
- All Playwright i18n tests pass
- Human approves visual quality of translated pages
</success_criteria>

<output>
After completion, create `.planning/phases/23-internationalization/23-02-SUMMARY.md`
</output>
