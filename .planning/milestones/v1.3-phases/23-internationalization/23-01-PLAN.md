---
phase: 23-internationalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/i18n/ui.ts
  - src/i18n/utils.ts
  - src/layouts/Layout.astro
  - src/components/Header.astro
  - src/components/LanguageSwitcher.astro
autonomous: true
requirements: [I18N-02, I18N-03]

must_haves:
  truths:
    - "Language switcher is visible in the site header on all pages"
    - "Every page includes hreflang link tags for en, es, fr, de, and x-default"
    - "Sitemap XML contains xhtml:link alternate entries for all locale variants"
    - "English URLs remain unchanged (no /en/ prefix)"
  artifacts:
    - path: "src/i18n/ui.ts"
      provides: "Translation dictionaries for en, es, fr, de"
      contains: "export const ui"
    - path: "src/i18n/utils.ts"
      provides: "useTranslations helper, getLangFromUrl, getLocalizedHref"
      exports: ["useTranslations", "getLangFromUrl", "getLocalizedHref"]
    - path: "src/components/LanguageSwitcher.astro"
      provides: "Language selector dropdown with locale persistence"
      contains: "getRelativeLocaleUrl"
    - path: "astro.config.mjs"
      provides: "i18n routing config and sitemap i18n config"
      contains: "i18n"
  key_links:
    - from: "src/components/Header.astro"
      to: "src/components/LanguageSwitcher.astro"
      via: "Astro component import"
      pattern: "LanguageSwitcher"
    - from: "src/layouts/Layout.astro"
      to: "astro:i18n"
      via: "getAbsoluteLocaleUrl for hreflang tags"
      pattern: "getAbsoluteLocaleUrl"
    - from: "astro.config.mjs"
      to: "@astrojs/sitemap"
      via: "i18n config block inside sitemap()"
      pattern: "i18n.*defaultLocale"
---

<objective>
Set up i18n infrastructure: Astro i18n routing config, translation dictionaries, useTranslations helper, hreflang SEO tags in Layout, sitemap i18n config, language switcher component, and Header lang prop threading.

Purpose: All i18n infrastructure must exist before translated pages can be created. Hreflang and sitemap must ship in the same wave as translated pages to prevent duplicate content SEO penalty — this plan creates the infrastructure that Plan 02's translated pages will use.

Output: i18n config, dictionaries, utilities, LanguageSwitcher component, hreflang-enabled Layout, sitemap i18n config
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-internationalization/23-RESEARCH.md

@astro.config.mjs
@src/layouts/Layout.astro
@src/components/Header.astro
@src/pages/index.astro
@src/pages/pricing.astro
@src/pages/use-cases/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n config, dictionaries, utils, and sitemap i18n</name>
  <files>astro.config.mjs, src/i18n/ui.ts, src/i18n/utils.ts</files>
  <read_first>
    - astro.config.mjs (current config — add i18n block and sitemap i18n option)
    - src/pages/index.astro (extract all user-visible strings for translation keys)
    - src/pages/pricing.astro (extract all user-visible strings)
    - src/pages/use-cases/index.astro (extract all user-visible strings)
    - src/components/Header.astro (extract nav label strings)
    - src/components/Hero.astro (extract hero strings)
    - src/components/Features.astro (extract feature strings)
    - src/components/HowTo.astro (extract how-to strings)
    - src/components/PricingPromo.astro (extract promo strings)
    - src/components/UseCasesTeaser.astro (extract teaser strings)
    - src/components/FAQ.astro (extract FAQ strings — or note if FAQ uses data file)
    - src/components/Footer.astro (extract footer strings)
    - .planning/phases/23-internationalization/23-RESEARCH.md (Pattern 1, 2, 3 for exact code patterns)
  </read_first>
  <action>
    1. **astro.config.mjs** — Add `i18n` block to defineConfig:
       ```js
       i18n: {
         defaultLocale: 'en',
         locales: ['en', 'es', 'fr', 'de'],
         routing: { prefixDefaultLocale: false },
       },
       ```
       CRITICAL: Do NOT set `redirectToDefaultLocale: true` — causes infinite redirect loop with `prefixDefaultLocale: false`.

       Update `sitemap()` integration call to include its own `i18n` config (sitemap does NOT auto-detect from Astro i18n config — see Research Pitfall 5):
       ```js
       sitemap({
         i18n: {
           defaultLocale: 'en',
           locales: { en: 'en-US', es: 'es-ES', fr: 'fr-FR', de: 'de-DE' },
         },
       }),
       ```

    2. **src/i18n/ui.ts** — Create translation dictionary file. Extract ALL user-visible strings from: index.astro (hero title, subtitle, CTA, meta title, meta description), pricing.astro (heading, subheading, toggle labels, tier names, feature lists, CTA buttons, FAQ if inline), use-cases/index.astro (heading, subheading, card titles, card descriptions), Header.astro (Pricing link text, Sign Up, Sign In), Footer.astro (all footer text). Also include nav strings: 'nav.pricing', 'nav.usecases', 'nav.signup', 'nav.signin', 'nav.dashboard'.

       Structure as typed const object per Research Pattern 2. English strings are the source of truth. Provide complete Spanish (es), French (fr), and German (de) translations for every key. Use accurate, natural-sounding translations — not word-for-word. Export `languages`, `defaultLang`, `ui`, and `UiKeys` type.

       The dictionary should have ~80-120 keys covering all three marketing pages plus shared nav/footer. Group keys by component: `meta.*`, `nav.*`, `hero.*`, `features.*`, `howto.*`, `pricing.*`, `usecases.*`, `footer.*`, `faq.*`, `common.*`.

    3. **src/i18n/utils.ts** — Create helper file per Research Pattern 3:
       - `getLangFromUrl(url: URL)` — extracts locale from URL pathname
       - `useTranslations(lang)` — returns typed `t(key)` function with English fallback
       - `getLocalizedHref(path, lang)` — returns locale-prefixed path (no prefix for 'en')

       Import from `./ui` only. Do NOT import from `astro:i18n` here (this file may be used in non-Astro contexts).
  </action>
  <acceptance_criteria>
    - astro.config.mjs has i18n block with defaultLocale 'en', locales ['en','es','fr','de'], prefixDefaultLocale false
    - astro.config.mjs sitemap() call has its own i18n config block
    - src/i18n/ui.ts exports languages, defaultLang, ui (with all 4 locales), UiKeys type
    - src/i18n/utils.ts exports getLangFromUrl, useTranslations, getLocalizedHref
    - ui.ts has 80+ translation keys per locale
    - All 4 locales have the same set of keys (no missing translations)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>Astro i18n config is active, sitemap has i18n config, translation dictionaries exist for all 4 locales, and utility helpers are exported and type-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Hreflang tags in Layout, LanguageSwitcher component, Header lang prop</name>
  <files>src/layouts/Layout.astro, src/components/LanguageSwitcher.astro, src/components/Header.astro</files>
  <read_first>
    - src/layouts/Layout.astro (current head section — add hreflang links)
    - src/components/Header.astro (add lang prop and LanguageSwitcher)
    - src/i18n/ui.ts (created in Task 1 — import languages)
    - src/i18n/utils.ts (created in Task 1 — import useTranslations, getLocalizedHref)
    - .planning/phases/23-internationalization/23-RESEARCH.md (Pattern 5, 6, 7 for exact code patterns)
  </read_first>
  <action>
    1. **src/layouts/Layout.astro** — Add hreflang link tags to `<head>` per Research Pattern 6:
       - Import `getAbsoluteLocaleUrl` from `astro:i18n`
       - Strip locale prefix from `Astro.url.pathname` to get the canonical path (regex: `/^\/(es|fr|de)/`)
       - Generate `<link rel="alternate" hreflang="...">` for each of en, es, fr, de
       - Generate `<link rel="alternate" hreflang="x-default">` pointing to English URL
       - CRITICAL: Include `x-default` — omitting it causes Google to show wrong locale variants (Research Pitfall 3)
       - Add `lang` prop to Props interface (default: 'en'). Set `<html lang={lang}>` dynamically instead of hardcoded "en".
       - Only emit hreflang tags on marketing pages (/, /pricing, /use-cases/) — not on /dashboard, /login, /signup, /api/* etc. Use a `showHreflang` prop (default: false) or check if the current path is a marketing path.

    2. **src/components/LanguageSwitcher.astro** — Create new component per Research Pattern 5 + 7:
       - Accept `currentLang` prop (string, default 'en') and `currentPath` prop (string, default '/')
       - Import `getRelativeLocaleUrl` from `astro:i18n`
       - Render a compact horizontal nav with EN | ES | FR | DE links
       - Each link uses `getRelativeLocaleUrl(code, currentPath)` for href
       - Current language gets `aria-current="true"` and visual active state (font-bold or underline)
       - Each link has `hreflang` attribute
       - Add inline `<script>` that writes `locale={code}` cookie on click (path=/, max-age=31536000, SameSite=Lax) using `data-locale-link` and `data-locale` attributes per Research Pattern 7
       - Style: text-sm, gray-500 default, gray-900 active, fits within Header's nav layout
       - Wrap in `<nav aria-label="Language selection">`

    3. **src/components/Header.astro** — Add lang prop threading per Research Pitfall 7:
       - Add `lang` prop to interface (default: 'en')
       - Import `LanguageSwitcher` and `useTranslations` and `getLocalizedHref` from i18n utils
       - Call `const t = useTranslations(lang)` at top of frontmatter
       - Replace hardcoded "Pricing" text with `{t('nav.pricing')}`
       - Replace hardcoded `/pricing` href with `{getLocalizedHref('/pricing', lang)}`
       - Add "Use Cases" nav link: `{t('nav.usecases')}` with href `{getLocalizedHref('/use-cases/', lang)}`
       - Replace "Sign Up" / "Sign In" text with `{t('nav.signup')}` / `{t('nav.signin')}`
       - Update signup/signin hrefs to use getLocalizedHref (these stay as /signup, /login — no locale prefix for auth pages)
       - Add `<LanguageSwitcher currentLang={lang} currentPath={Astro.url.pathname} />` between the Pricing link and the auth buttons
       - Ensure LanguageSwitcher only renders on marketing pages. Pass a `showLanguageSwitcher` prop or always show it (acceptable for v1.3 — all pages can have the switcher).
  </action>
  <acceptance_criteria>
    - Layout.astro generates 5 hreflang link tags (en, es, fr, de, x-default) in head
    - Layout.astro html tag uses dynamic lang attribute
    - LanguageSwitcher.astro renders 4 language links with correct locale URLs
    - LanguageSwitcher writes locale cookie on click
    - Header.astro accepts lang prop and uses useTranslations for nav text
    - Header.astro nav links use getLocalizedHref for locale-aware paths
    - Header.astro includes LanguageSwitcher component
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/ranjit/Documents/Development/qr-generator && npx astro check 2>&1 | tail -20 && npx astro build 2>&1 | tail -30</automated>
  </verify>
  <done>Layout emits hreflang tags, LanguageSwitcher component exists and persists locale via cookie, Header shows translated nav text and includes the language switcher. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `npx astro check` passes with no errors
- `npx astro build` succeeds — generates static output
- Built HTML for `/index.html` contains `<link rel="alternate" hreflang="es">`
- Built HTML for `/index.html` contains `<link rel="alternate" hreflang="x-default">`
- Header contains LanguageSwitcher with 4 language links
</verification>

<success_criteria>
- Astro i18n routing configured with prefixDefaultLocale: false
- Translation dictionaries exist for 4 locales with 80+ keys each
- useTranslations helper is type-safe with English fallback
- Layout emits hreflang tags including x-default
- Sitemap integration configured with i18n locale mapping
- LanguageSwitcher component renders in Header with cookie persistence
- Header nav text uses translations, links use locale-aware paths
- Build succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-internationalization/23-01-SUMMARY.md`
</output>
